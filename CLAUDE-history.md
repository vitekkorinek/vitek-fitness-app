# CLAUDE-history.md — Changelog & rationale

**Context only — not active instructions.** Active rules live in CLAUDE.md and the per-area companions. Come here for *why* something is the way it is, or to check whether an approach was already tried and rejected.

## Recovering the full pre-July-30 2026 docs

Until July 30 2026, CLAUDE.md carried an 18-entry "RESUME HERE" block (~98k chars) and SPEC.md carried a 190k-byte §7 "Screen Map". Both were retired that day: their durable rules were merged into the companions, and the rest was narrative or already stale. **Both are permanently recoverable from git, byte for byte** — there is no need to keep a second copy here:

```bash
git show a3780f8:CLAUDE.md
```
```bash
git show a3780f8:SPEC.md
```

Use those when you need the exact wording of a July 2026 decision, or per-screen detail for a screen whose companion coverage looks thin. Anything still *true* lives in CLAUDE.md and the companions — **where the old text disagrees with a companion, the companion wins.**

## ⛔ Rejected en route — do not re-propose

The expensive part of the archive: things that were built, reviewed on device, and turned down. Re-proposing any of these costs Vitek a round of his own testing time.

**Workout & routine cards**
- **Routine card:** green outline · beige/green diagonal gradient washes ("dirty") · strips-on-dark with a white-footer-name flip · exercise-list-style cover with inline marks, ring-right and 2×2 strip cells — **all rejected across ~6 device iterations.** The surviving anatomy is in CLAUDE.md §8.
- **The Active/Closed pill and the "THIS WEEK X/Y" goal row were removed from My Routines** — redundant beside the Active/Closed filter; the goal row belongs to the workout gallery.
- **Light covers must stay flat white** — a mirror of the dark gradient was tried and dropped the same day.
- **Muscle tinting on the cover silhouette** — tried at three strengths, rejected. The category hue lives in the pill only.
- **The all-dark seamless card** was retired everywhere once every card gained the contrast-footer anatomy; a seamless dark card then reads as inconsistency, not emphasis.
- **`ActiveRoutineCard` v1–v3** (compact white row · dark card with the name list as cover text · one-zone tile with the pill row under the name) — all rejected before Vitek sketched v4, which itself was later replaced by the unboxed `RoutineReadout`.

**Do Mode**
- **Trainer `?viewOnly=1` on the IN PROGRESS card** — reverted within the hour. Trainer-led Do Mode on his own phone is the *normal* case (one-on-one PT), and view-only actively caused a duplicate session.
- **Reopening a paused rest timer from "Start timer"** — reverted the same evening: *"start timer means start timer."* A paused rest is reachable via the mini pill.
- **Bold reps in the set row** — at `700`, reps and kg differ by 1px and the row reads flat. The size/weight step *is* the hierarchy.
- **A separate pre-session screen** (`session-intro.tsx`) — deleted July 26 2026, never rebuild it.
- **The merged sliding preview panel + slideshow backdrop** — deleted July 31 2026 after five days live: a real client pressed the big green "Start session" button without meaning to start, and the drag-up-to-review gesture was undiscoverable. Replaced by the trainer-style direct landing (START pill in the header, pre-start banner state). Do not rebuild a pre-session panel or screen in any form.
- **The in-screen finish-retry interval, the "Waiting for connection…" footer, and resume-an-owed-finish-on-load** — all superseded by the outbox. They made the client wait for a connection instead of letting the session end.
- **Warm-up sets as a single sequential run with display-only renumbering** — rejected: flagging a warm-up onto an existing workout would shift set 1's logged history onto set 2.

**App-wide**
- **Scroll-to-top on tab re-entry** — the old reset was an accident of the spinner unmounting the ScrollView, never a decision. Do not "fix" it back (see CLAUDE-infra.md).
- **Calling `fetchUpdateAsync()` at launch**, and relaunching on `isUpdatePending` alone — both wrong, both replaced. See CLAUDE-infra.md "OTA updates".
- **Client-body photos as workout covers** — rejected on body image and consent/GDPR grounds.
- **The mint-green header wash** — rejected; one neutral tint app-wide. Revert flag `GREEN_TINT` in `components/LightHeader.tsx`.
- **A web build** — the export is broken and Vitek does not want one. Always ship `--platform ios`.

## Changelog

> **Favourites landing became the tap-to-expand HEART ORBIT and the dark meal/recipe fallbacks went light (4 Aug 2026, one long session; `favourites.tsx`; device-perfected through ~9 OTA rounds).** A client's report started it — *"the cards are too dark especially when they are many under each other like in meal"* — and the diagnosis held: the Days list (white cards, colour as a small badge) worked while photo-less meal cards stacked navy `#2e4288→#1d2d6a` slabs into a wall; the July dark-tile landing had the same weight. **List fix (pushed `d04268e`):** photo cards untouched; no-photo meal/recipe cards get a LIGHT same-hue wash + watermark + ink text (allowed despite the workout-cover flat-white rule — tint-to-deeper-tint, not a white wash); recipe detail/editor keep dark amber hero covers deliberately; `TileCard`/`FullWidthCard` deleted. The Days heart badge went plum to carry its folder hue (`19ec11d`; a pink card wash was rejected — reading cards stay white). **The landing then walked, all in one day, through:** a STATIC hub (Recommendations filled center; the brown was killed on sight, Foods → teal when the center took green) which Vitek approved but *"something is not sitting right"* → he returned to his first-message **heart idea**, felt an interactive widget mock (the mock's tap-to-orbit + the Food-Log-FAB-style morph sold it), and chose full tap-to-expand over Claude's auto-bloom compromise, accepting the discoverability cost (a muted "Tap the heart" hint mitigates). Iterations on device: flex rows put the heart below center → rebuilt as an absolutely-positioned **pentagon** (fly-in delta = negated offset, the `measureInWindow` plumbing deleted the same hour it was written); "more airy" → heart 96→80, wider ring; bottom pair evened twice (~163pt → ~127pt from the heart, then pulled toward each other); circles 96→88. **The colour endgame — Vitek's own insight:** the Food Log's macro/micro pips already own "colourful circles with icons", so the colourful orbit dressed two systems identically, and Reco's dark fill "made no sense" inside the ring. Three treatments mocked (all-dark-gradient — declined as the heaviness complaint reborn; monochrome light green; colourful + grey Reco) → **"lets go b": every orb the same pale HEADER-green tint, the ACCENT heart the only saturated mark**, open heart = ORB_BG fill + light-weight HEADER outline. The per-folder tints (amber/navy/teal/plum) lived on the landing for about two hours; the teal-Foods decision died with them. Full spec in CLAUDE-nutrition.md. Typecheck clean throughout.

> **Set rows rebuilt to Vitek's own spec: TOTAL out, swipe-to-delete, per-row `+`, ramp sets, comma decimals kept (4 Aug 2026; both Do Mode files + builder + SessionDetailsSheet + new `lib/weightInput.ts`; committed + pushed + OTA'd same morning).** Vitek opened with the problem list — TOTAL "most needed only for barbell … for dumbbell it multiplies by two" (wrong when he programs single-arm work), the ✕ eating row space — and proposed the shape himself over three messages: expanded cards stop swiping (collapsed keeps done/Replace/Add-below), rows swipe-left to delete, a contextual `+` per row replaces the card's add menu (the toolbar `+` became the camera), and warm-up conversion runs top-down from row 1 so "suddenly that is set 1" and the option reappears. His ramp-set idea (ascending warm-ups done back-to-back, "visually as a drop set … we are not dropping, adding") cost zero schema: `isDropset && isWarmup`, inherited from the chain parent — one `addDropset` makes both kinds, and the graph/chips exclusions fall out free. Three device rounds: the in-flow "= total" hint "lifts the field up" → absolute `top:'100%'` + uniform `inlineSetRowBar` padding; then the dashed added-sets divider and the grey dropset-row background both dropped on sight. En route: his 27,5→27 report (total 54 not 55) was the comma decimal-pad vs `parseFloat` truncation — `parseWeightInput` now guards every typed-kg parse app-wide; and the "Access workout_sets" policy turned out `FOR ALL` since April, so the client-side warm-up conversion persisted with no migration. Same session Vitek ruled both `exercise-detail.tsx` files **inactive legacy code** — kept, never ported to again (rule at the top of CLAUDE-domode.md). Full spec: CLAUDE-domode.md "Set rows — Aug 4 2026 rework".

> **The app icon settled in two rounds, then became a PICKER (3 Aug 2026, evening; picker build-ready, NOT built).** Build 41's dark tile read heavy on Vitek's real home screen ("too dark and the vf logo is too big"); the fix ran through ~10 HTML comparison pages in chat (the mockup-first lesson applied to icons): white background won, the mark went 58% → "too small" on device → 62%, and a gradient INSIDE the mark (his idea) landed at the "in between" strength `#2F947A→#244E43` — en route, pixel-sampling his original icon revealed its gradient ran dark-top→light-bottom, which is why it had always felt off to him. His half-logo/tile-watermark idea was tried three ways and he killed the crop himself ("nothing on the other side to balance it") but kept the ghost TREATMENT centred at normal size — the "Shadow" icon. Final set: Classic (default) · Dark · Bright green (his original's colors, direction flipped to standard) · Shadow. Then "wire the picker": `expo-alternate-app-icons` (iOS-only entries, validated via clean prebuild — ⚠️ a DIRTY prebuild crashes in `@bacons/apple-targets`' update path, `removeFromProject` of undefined; always `--clean`), assets + SVG sources in `assets/app-icons/`, `lib/appIcons.ts` with the liveActivity-style optional-require so the OTA'd JS hides the row on builds without the module, picker sheets mirroring the card-style picker on both sides. Spec in CLAUDE-screens.md "Client Me tab — Appearance". Same evening, on his "push everything and ship": the other session's island/free-session work was committed (its last 4 files landed as `44d38e0` after a near-collision — the trainer Do Mode file changed UNDER the commit sequence while that session was still writing; the build was held until he confirmed it finished) and build 44 went out with the 62% icon.

> **Free sessions grew up: inline Add bar, a rename that sticks, a category, and a face for their cards (3 Aug 2026, late evening; both Do Mode files + CategoryCover/WorkoutPaperCover/outbox/draft + trainer client profile).** Vitek's four-part device report on the free session: the floating `+` circle → a **long filled-green squared "Add exercise" bar** (empty-state middle, then always under the last exercise, above the Finish pill); *"we cant rename the session"* → diagnosed as PERSISTENCE, not the pencil — the row's `name` is stamped at START, `load()` read it back on any suspend/resume and the outbox UPDATE branch never wrote it, so mid-session renames silently reverted (his 1h free session that day still carries the default name in the DB) — fixed by write-through on Confirm + name/category in the session draft + `name` in the outbox UPDATE; the rename sheet also **assigns one of the 8 standard categories** (no stretching — his spec), which drives the banner live and lands on the backing workout at FINISH; and **uncategorised workouts now draw a PLAIN neutral silhouette** (`NEUTRAL_CFG` in CategoryCover; `categoryHasCover` deliberately unchanged so the legacy header's photo branch can't flip). The *"exercises done in the free session dont appear on top of the card"* half had a second cause on the trainer side: the client profile's `exerciseNamesMap` fed only `status='active'` workouts, and a free session's backing workout is `'completed'` — its week-strip/recent-activity covers rendered blank; session `workout_id`s are in the map now (the client Training tab was already correct, fetching names per session). Full rules in CLAUDE-domode.md "Free sessions — rename WORKS mid-session now". **⚠️ Repo oddity worth remembering:** the parallel island-clock session committed+pushed `5b700bd` MID-implementation, sweeping the half-done client file (and writing a commit message describing it) while the libs it references were still uncommitted — so that pushed snapshot alone doesn't typecheck; the remaining four files (trainer Do Mode, sessionDraft, sessionOutbox, trainer client profile) complete it. Typecheck 25, baseline unchanged. **Follow-up the same evening (device-confirmed: "the free session works"):** Vitek wanted the free session's name/category editable AFTER the session too, "same treatment as the other workouts" — solved by extending the week-strip session ⋯ menu's **Edit workout** row to COMPLETED sessions (his broader ask: edit from the week strip "for any workout"; in-progress stays excluded — editing under a running session — and rows with no backing workout hide it via `hasWorkout`). Doing so exposed that the row had **always pushed a `workoutId` param the builder doesn't have** (it takes `editWorkoutId`) — the scheduled-session Edit workout had been silently opening an empty builder. Spec in CLAUDE-screens.md "Session card ⋯ menu".

> **The island learned to share with Music — build 43 (3 Aug 2026, evening; submitted, device-confirm pending).** Vitek's report from the floor, with a home-screen screenshot: *"we have a problem with a dynamic island when the music plays which is often when the person trains alone"* — when Music holds one side of the island, iOS demotes the activity to the tiny `minimal:` slot, which showed only the hourglass/timer glyph; his proposal, built as specified: **the TIME, green for session / white for rest** (red overtime kept). A follow-up added the glyph back beside it (9pt icon + caption2 digits, template-sized, scale floor 0.5 — a miniature of the compact slots). Then his width question (*"perhaps we can make 90min instead of 1:00:00? or thats messy"*) turned into the second change: **session digits are total MINUTES island-wide + lock card** (`90:23`) via `Text(timerInterval:countsDown:false, showsHours:false)` — a native API flag, not a hack; the REST clock must stay `style: .timer` (timerInterval clamps at zero = no overtime count-up); **the in-app chip deliberately keeps `01:30:23`** (*"for me its ok that the dynamic island shows 90 and the app says 1:30"*). Final app icon (white bg + gradient mark, from the icon session) rode along. Validated by `swiftc -typecheck` only — no JS/module change, so 40–43 share the runtime. **The build number says 43 because 42 was burned by a process failure, not a build:** Claude kicked off the build unprompted the moment the first tweak landed and Vitek had to interrupt (*"please dont make builds without me agreeing on it, i have limited amount of builds"*); cancelled in-queue (~22s, before compiling — normally not billed) but EAS's remote counter had already incremented. **The rule — finish → report → HE approves → HE says build — is now in CLAUDE.md §8 and memory; no standing plan or "this work was waiting for a build" counts as authorization.** The real build ran only on his explicit "ok make the build now".

> **A finished session was deleted by its own leftover header chip — diagnosed from the DB, fixed in both Do Mode files (3 Aug 2026).** Vitek, after three sessions that day: Oscar fine, Adam Test's free session fine, then Bastian Mödler's *"Unilateral push"* — *"it got saved but then somehow the countdown of the session still appeared in the header and when i clicked on it it brought me back to the session where it was running but nothing was checked … so i decide to discard the running session and that deleted the running session and also the session from the week strip."* The DB confirmed the worst reading: **no `sessions` row and zero `session_logs` survive for that workout** — Oscar's (25 logs) and Adam's (13) are intact. Two `notes` rows written live during the session dated it exactly (15:53 set note *"Right was only 7"* on Shoulder Press – Single Arm; 16:31 exercise note *"Try 15kg"* on Lateral Raise), and the "randomly saved weights" he saw afterwards were the builder's `workout_sets` targets plus cross-workout prefill, not survivors. **The chain:** he left Do Mode mid-session once ("Leave — keep it running" → `suspendSession`), came back via the week strip's **IN PROGRESS card**, which `router.push`es Do Mode and clears nothing — and `saveSession` **never called `clearSuspendedSession()` in either end branch**, so FINISH left the chip ticking on a now-`completed` id. Tapping it reopened Do Mode via `resumeSessionId`, blank because the draft had just been cleared and Do Mode never reads `session_logs` back into the inputs; **Discard's unguarded `delete().eq('id', sid)` then took the completed session and its logs with it.** Unrecoverable — the Supabase org is on the free plan, so no PITR or backup, and the phone's copy was cleared at finish and flushed from the outbox. Fixed three ways (rules in CLAUDE-domode.md "Session survival"): `clearSuspendedSession()` in both `saveSession` end branches; discard gated on the new `lib/sessionGuards.ts` **`isSessionStillRunning`** + `.eq('status','in_progress')` on both mutations, **failing CLOSED** so an unreachable server leaves a stale `in_progress` row rather than risking a finished one; and `load()` clearing a matching chip (client AND workout) when it adopts a running row, so the week-strip entry path can't leave one alive. Both Do Mode files. Typecheck 25, baseline unchanged. **Worth keeping:** the suspended-session chip is cleared only by whatever CONSUMES it, so any new way into a running session must either clear it or be covered by the `load()` rule — and every `delete()` addressed by a session id needs to ask what state that session is in first.

> **The rest-end APNs Live-Activity push built end-to-end — the locked-phone red flip (3 Aug 2026, build 40; DEVICE-CONFIRMED same day: "the timer works, it goes red now". Vitek created APNs key LLV7MWGYUT — the Expo key can't be exported — and a fake-token smoke test proved the JWT auth before the device try).** The morning's appointment-push question resolved first: Adam's kettlebell tray showed the notification but no push arrived — NOT a bug, the by-now-familiar one-phone rule in its sharpest form: the appointment-created push fires the INSTANT the trainer saves, when the phone is by definition signed in as trainer (`sent-push` ran 1s after the insert; Adam's token appeared 40s later at his login). Claude re-fired the identical payload while the phone was signed in as Adam → `sent:1` → **Vitek confirmed the push on device — the appointment pipeline is now fully device-proven.** Queue-and-deliver-on-login was discussed and declined: the tray IS the queue; a push delivered at login arrives while the user is already in the app. Then his rest-timer report (*"the timer is white then sometimes it gets red with the minus"*) — diagnosed as the documented staleDate-doesn't-repaint gap, NOT a build-39 regression (that commit is 4 lines of compact-island layout); vibration inconsistency attributed to iOS attention-awareness/silent-switch, not the app. On his "yes start it": the full APNs rest-end push. App side (build 40): `pushType: .token` with pushless fallback, token → JS event → new `lib/restActivityPush.ts`, ONE pending row per user upserted at rest start/resume + deleted on pause/stop/end + refreshed on progress change; **ContentState's two dates became MILLISECOND DOUBLES in both Swift copies** to kill the Codable-Date-over-JSON ambiguity before it could bite. Server side (live already): `live_activity_rest_pushes` table, `get_service_secret` vault RPC, `live-activity-push` edge fn (claim-by-DELETE → wait to the exact second → raw APNs ES256 push, sandbox fallback), pg_cron every 5s gated on an indexed EXISTS. Dry-tested end-to-end with a fake row: cron picked it up in <5s, fn claimed it, stopped at the expected "apns key not configured". Remaining: Vitek creates/downloads an APNs .p8 (interactive-only, `eas credentials` can't export headless), Claude vaults it, then the device test. Spec in CLAUDE-infra.md "Live Activity" (new bullet) + "Server push notifications".

> **Notifications settled: appointments are THE notification — and the tray write moved into the database (3 Aug 2026; everything device-confirmed except the appointment-sent push).** The morning opened with device confirmations: build 38's lock card renders, the island's red overtime works, and the first real cron firing delivered — the 00:30 test appointment's reminder went out at 00:05 Berlin, trainer copy received. The client copy went nowhere and that was CORRECT: one phone, two accounts — sign-out deletes the account's push token ("push follows the account"), and at send time the phone was signed in as trainer, so Adam Test had zero tokens (`sent:0, noToken:1`). Build 39 fixed his island nit (*"isnt the time a bit too far away from the edge?"*): the compact session clock was the LAST raw greedy `Text(style:.timer)` in the widget — it filled its 46pt frame digits-left, dumping its slack as a right-edge gap; now on the hidden-template pattern like every other clock; confirmed on device. Then his correction — *"they do [have an in-app place], we already built it before — in the header in the kettlebell or pear icon"* — exposed the real find of the day: the 00:30 appointment's TRAY row was never written. The trainer's phone wrote `client_notifications` fire-and-forget at "Save & send" (no error check, no retry) and one dropped request at 23:49 lost it silently, while the DB write path replayed perfectly healthy under his RLS context (the only failing shape is `INSERT…RETURNING` — the trainer can't read a client's row back; the app's `return=minimal` shape is fine). Fix: **tray rows are now written by the DB trigger `appointment_sent_notify`**, atomic with the appointment write, from every path; all six app-side inserts removed (schedule.tsx ×3, plan-week.tsx ×3). En route the same trigger work fixed a live bug nobody had hit yet: the session-assigned push's dedupeKey was per-ROW, and a repeat-weekly plan inserts up to 52 `sessions` rows — 52 pushes. Then the product decisions, in order: a workout-ADDED notification offered → *"might be too much… let me think"* → **"i would take the notification for the planned workout out completely. only for the appointment"** — the whole session-assigned notification (push + tray, shipped 2 Aug, never fired for a real client) DROPPED (`remove_session_planned_notification`); and the counterweight, on his *"yes of course, they have to be notified when they get an appointment"*: **appointment SENT now also PUSHES** (`appointment_sent_push`, dedupe per appointment id — validated in a rollback transaction: 1 insert → 1 tray row + 1 queued push; ⚠️ NOT yet device-checked, the one piece of the day Vitek hasn't seen work). Confirm stays tray-only. Also OTA'd: the Schedule + Plan Week time-picker sheets got `avoidKeyboard` — the keyboard covered their START/END inputs (*"cant make appointment if i want to write the time"*). Rules in CLAUDE-infra.md "Server push notifications" + CLAUDE-schedule.md.

> **Phase 4 COMPLETE except the activity push — all three notifications live and the pipeline proven on device (2 Aug 2026, late night).** Same evening as the foundation: `scheduled-notifications` edge fn + two cron jobs + the `session_assigned_notify` trigger (details in CLAUDE-infra.md "Server push notifications"), service key moved to Vault. Tested with dryRun/force against live data (availability: 6 clients → 4 ask / 2 have, next-week Monday computed right) — and then a REAL push sent through the full pipe to Vitek's phone (signed in as client Adam Test, whose token the OTA'd registration had already captured): delivered. His "i didnt get any notifications" mid-build was correct behavior — neither cron was due; the test push answered it better than an explanation. Island polish continued in parallel: build 36's alignment fix exposed that `Text(style:.timer)` is GREEDY (takes all offered width so it never re-layouts — the timer glyph floated mid-card away from the digits); build 37 tried `.fixedSize(horizontal:)` and the greedy text's "ideal" width **blew the layout up — the lock card rendered as an EMPTY green rectangle on device** (Vitek caught it before midnight); build 38 lands the known-good pattern — a hidden static digit template sizes the box, the live timer paints in its overlay (rule + why in CLAUDE-infra.md). Red overtime `-mm:ss` confirmed working on device on both island and card (via the app-alive repaint). Remaining: the locked-phone exact-second red (Live-Activity APNs push — needs raw .p8 key + pushType build) and the client-rollout caveat: **clients on build 30 get no pushes until they update from TestFlight.** Started on Vitek's go ("yes start phase 4"), right after build 36 went out with his lock-card alignment catch (the fixed-width frames around the self-ticking `Text(style:.timer)` — removed, glyph alignment via `multilineTextAlignment`). Shipped: `push_tokens` + `notification_log` tables, the **`send-push`** edge function (claim-then-send idempotency — tested live: first call `noToken:1`, identical second call `skipped:1`), `lib/pushTokens.ts` registration in AuthContext, and a sign-out gap fixed in passing (rest timer + Live Activity survived logout). Also settled on device this round: **red overtime while locked is impossible app-side** — repaints are forbidden while the app sleeps, so the render-time check only helps when the app is awake; the exact-second flip moved to Phase 4 as a Live-Activity APNs push (needs a raw .p8 key — Expo's push API can't address activity tokens). Remaining Phase-4 work — the two crons, the session-assigned trigger, the activity push — tracked in memory, next session.

> **The lock card became a designed object — build 35 (2 Aug 2026, COMMITTED & PUSHED with the whole notifications phase; design + red overtime confirmed on device).** Vitek's ask list for the Live Activity card, settled over FIVE mockup rounds in chat before a single line of Swift (his own suggestion — *"does every new notification window need a new build? if yes then we should perhaps do mockup and then decide"* — and the right one; this would have been five builds): logo + app identity like other notifications · options A–D solid + E system-glass after he asked *"arent the notifications usually see through with apple liquid glass?"* (answer: regular notifications yes; Live Activities are the one card Apple lets apps brand — most apps go solid) · **C "green depth"** chosen for branding (the workout-cover gradient family) · count moved beside the name ("Push Day 3/8") · NOW/NEXT exercise lines · then his final structural call: **REST always visible (dimmed 0:00 when idle) so the card never changes shape, logo alone top-right, no "Vitek Fitness" text**. Built exactly that, plus: `updateProgress` in the module/state (NOW follows the open card, NEXT = next not-done, deduped — exercises' identity churns per keystroke), **revive-after-clear** (his catch: clearing the card killed the island with no way back — `hasActiveActivity` + foreground revive in store + both Do Mode files, lib re-pushes cached progress), and the **staleDate finding**: build 34's red flip NEVER fired on device — `isStale` marks but does not repaint (Apple forums 740406/759250), so the widget now checks "over" at repaint time and the store's rest-end timeout forces the repaint while the app is alive; locked-and-untouched stays green until the next repaint (accepted; APNs Live-Activity push in Phase 4 could close it). The VF logo: `assets/images/icon.png` turned out to still be the EXPO PLACEHOLDER — the real mark lives only in `components/VFIcon.tsx` SVG, rasterized to the widget's xcassets via sharp-cli (NSImage silently refuses file-based SVGs). His overtime wish "-00:03" ships as a red minus prefix on the same repaint rule. Also his re-test lesson from 33 applied: told him explicitly to wait for BUILD 35 in TestFlight.

> **…superseded within the hour by build 34 (island v3):** Vitek re-tested BEFORE build 33 had cleared Apple processing — the "still the same" report was v1 still on his phone, worth remembering as a failure mode now that builds auto-submit: **a native change is NOT on the device minutes later the way an OTA is; check the build number before acting on a report.** His two genuinely new asks went straight into build 34: **rest clock flips RED in overtime** — via `staleDate` = rest end, so the system re-renders (`context.isStale`) the moment the countdown expires, phone locked included, no update push — and **icons beside BOTH times** (hourglass by the rest, timer glyph by the session, on compact + expanded + lock screen). No JS/module-API change, so 33 and 34 share the same runtime and OTAs.

> **Island v2 + the rest-over notification (2 Aug 2026, build 33 — device-confirmed "it works with the 33"; COMMITTED & PUSHED with the phase).** Vitek installed build 32 (his one interactive `eas build` run created the widget profile AND — answering yes to the CLI's offer — the APNs push key + a stored ASC API key, so every later build/submit is fully non-interactive) and the island worked first try. Two device findings: **(1)** *"the timer is on the right side and the countdown is on the left side, its confusing"* — v1 moved the session clock from trailing-green to leading-white when a rest started, i.e. the one always-present clock changed both side and color exactly when a second clock appeared. v2 rule, now also in CLAUDE-infra.md: **the session clock never moves and never recolors (right, ACCENT, always); the rest joins LEFT as hourglass + white digits** — the same sides as the in-app header chips; expanded + lock screen follow. **(2)** *"when the timer is over there is no vibration nothing happens and the timer suddenly start adding seconds"* — the in-app vibration is JS and the phone was locked; the count-up past zero is the (intentional) overtime display, but nothing MARKED the moment. Fix = the Phase-3 piece, shipped in the same build since expo-notifications was now in it: `lib/restNotifications.ts`, a local notification at rest end, armed/cancelled in lockstep with the store's vibration timeout, permission asked on first rest start, no foreground handler on purpose. This collapsed Phase 3 into Phase 2's follow-up build; remaining phase work is Phase 4 (server pushes) only.

> **Notifications phase, Phase 2: the native build prepared — expo-notifications + the workout Live Activity (2 Aug 2026; became build 32 via Vitek's one interactive `eas build` run — the island worked first try; COMMITTED & PUSHED with the phase).** Everything native the whole phase needs, batched into one build so Phases 3–4 stay OTA: `expo-notifications` installed (+ `aps-environment` entitlement — the old profile had NO push entitlement at all, verified by decoding build 30's embedded.mobileprovision, which also yielded the `appleTeamId` SGZ83SR8YV without bothering Vitek), and the **Live Activity**: local Expo module `modules/live-activity/` (start/updateRest/end; one activity at a time, stale ones ended on start) + widget extension `targets/widgets/` via `@bacons/apple-targets` (deployment 16.2; `WorkoutActivityAttributes` deliberately defined twice — see CLAUDE-infra.md "Live Activity" for the sync rule). Island design per Vitek's earlier call: both clocks — compact shows timer-glyph + session clock; during a rest the session clock steps to leading and the rest countdown takes trailing in ACCENT; clocks tick natively (`Text(style: .timer)`, zero updates, free overtime count-up). JS wiring shipped in the same round (store rest actions sync the island; Do Mode starts/ends the activity with the session — never ended on suspend). **Version 1.0.0 → 1.1.0** so the new runtime isolates OTAs from the module-less old build. Validation without an EAS burn: local prebuild + `pod install` (CocoaPods via Homebrew; `LANG=en_US.UTF-8` or it crashes in its own error reporter) confirmed the target embeds and the module autolinks; `xcodebuild` was a dead end (iOS platform component not downloaded on this Mac) but `swiftc -typecheck -parse-as-library` against the iphoneos SDK cleared the widget Swift. The non-interactive `eas build --auto-submit` then failed exactly where predicted: the NEW widget target needs its provisioning profile created once with an Apple sign-in — handed to Vitek as a single command; every later build is non-interactive again. Typecheck 25, baseline unchanged.

> **Notifications phase, Phase 1: the rest countdown survives leaving Do Mode (2 Aug 2026 — both rounds device-confirmed; COMMITTED & PUSHED with the phase).** The phase was scoped in conversation first: (1) rest timer persisting app-wide, (2) ONE native TestFlight build batching `expo-notifications` + a Live Activity widget extension (Dynamic Island — both clocks via `Text(timerInterval:)`; compact view shows session clock in one slot and rest countdown in the other, so "which one wins" turned out to be "both"), (3) local rest-done notification + Live Activity wiring, (4) push notifications via Supabase (30-min APPOINTMENT reminder to trainer + client; availability reminder Thu 12:00 + Fri 12:00 Berlin — **everyone gets it, two messages**: "please send by Fri 15:00" vs, for fixed-schedule/already-submitted clients, "I have yours for next week — changes by Fri 15:00", per Vitek's call; session-assigned push via DB trigger). **Phase 1 shipped in this round:** the countdown moved from local state + `setInterval` in each Do Mode file into `store/sessionStore.ts` as an absolute `endsAt` timestamp (`restTimer` + `start/pause/resume/stopRestTimer`, `pausedRemainingSecs` may go negative for an overtime pause), with `useRestTimerTick()` as the one render hook and the end-of-rest vibration owned by the store (module-level timeout) so it buzzes anywhere in the app. Both Do Mode files derive their old `rest*` names from the hook — `RestTimerSheet` and the running pill are untouched; `stopRestTimer()` added to both FINISH end-branches and Discard, and the old unmount cleanup deleted (surviving unmount is the point). V1 made the rest countdown REPLACE the elapsed time in all six session-chip sites; **Vitek confirmed it worked on device, then redirected the design the same day** (*"it works … i would go one step further. lets have the timer on the other side and perhaps different icon next to it, clicking on it it shows the timer running, we can also stop it from there or go back to the session, the countdown of the session keeps behaving the same it behaved till now"*) → **v2: the session chips were all reverted to elapsed-only, and the rest got its own `components/RestTimerChip.tsx`** — hourglass glyph + countdown at the header's LEFT (`left:66` mirroring the session chip's `right:66`), tap → glass popup with the live clock + "Back to session" + "Stop timer". Wired through `LightHeader`'s default overlay slot (sibling of `SessionResumeChip`; custom-overlay screens — tabs `_layout`, nutrition, trainer client profile — add it explicitly; the home hero uses a mirrored `hero` pill variant at left:20; the v1 "Rest ·" lines added to the session popups were removed again). Timestamps also fixed a latent drift: backgrounding froze the old interval, so the countdown silently stalled — now it recomputes from the clock. Typecheck 25, baseline unchanged, both rounds.

> **Log-training sheet gets a centred head, a backed amber banner and its glyphs back — plus yellow dumbbells for planned days (2 Aug 2026, COMMITTED & PUSHED, OTA’d twice).** Three enhancements Vitek asked for in one message, all in the client Training tab (`app/(client)/(tabs)/train/index.tsx`). **(1) The sheet head is centred like the routine info panel** — *"can we have the log training title in the middle as we have it for example with detail of routine panel then today under it centered"*: title 16/700 left → **17/700 centred**, "Today"/"<Weekday D Mon>" centred under it, hairline divider below the pair. That is the `RoutineInfoSheet` treatment and the app-wide sheet-title rule; the sheet had simply never been converted. **(2) The 48h rest hint became a filled amber banner** — *"can this be done the same way we have it in routine card its yellow but with background you can see it in the third screenshot"*, pointing at the plan-mode `planBanner` strip on routine detail. Same block reused: `#fdf3e2` ground, radius 12, a `moon.zzz.fill` glyph, `#8a5f12` text at 12.5. As bare amber text (with a negative top margin hugging the subtitle) it read as a stray sentence; backed, it reads as the day's status. The **green Recommended line stays on the "any workout" row** — that arrangement was Vitek's own from July 24 and nothing about it changed. **(3) The option rows have leading SF Symbols again** — *"i know we said no icons but here it will work well, the same goes for planning"*: `plus.rectangle.on.rectangle` / `arrow.triangle.2.circlepath` / `timer`, mirroring the trainer's Add Session menu. **The July removal that this reverses was about EMOJI (🏋️/📋), not glyphs** — a distinction worth keeping straight, since the same note also carries the free-session row's still-live rule (dark label, colour only on the sub-line). So the glyphs are all HEADER dark green, including free session's, where the trainer's is ACCENT: the client list must not single out a row by colour. One icon per row across both the Log and the Plan variant — the glyph names the subject, the label names the mode. **(4) The month calendar now marks planned days** — *"can we have a yellow dumbbell for planned workouts? so the planned workouts also appear in the calendar"*. `loadCalModalSessions` was completed-only; it now selects `date, status` over `completed` + `scheduled` and splits into two sets, with a second legend entry. **A day holding both renders green** — the completed session is the stronger fact about that day, and it matches the week strip, where `selectedSession` also prefers completed over planned. `in_progress` is left unmarked: the strip already surfaces a live session on its own day, and a third colour in a two-colour legend buys nothing. Typecheck 25 errors, unchanged from baseline. **Device-reviewed the same evening — "all works well"** — with one follow-up: **(5) the month grid now spills into the neighbouring months.** *"here i have a july month but in this week there are two days from august, is there a way for me to display it there?"* July 2026's last row ended at Friday the 31st, so Sat Aug 1 — a day he had trained — simply wasn't on screen. New `buildFullCalendarGrid` fills those cells with the real days, muted in the number but **full-strength in the dumbbell**, tappable like any other. The fetch bounds had to move with it: they read off the grid's first and last cell now, since a month-bounded query would render the overflow days blank and look like a bug rather than a gap. The move-training calendar was deliberately left on the old blank-padded helper — its cells carry month-scoped disabled/selected state. Dead `calNow` removed with the old day-cell code.

> **The Cancel row left the sheets (2 Aug 2026, COMMITTED & PUSHED `6965dcb` after Vitek confirmed on device — "it works now").** Vitek, after the sheet-title work: *"only a design question, you do always this cancel and under it leave a lot of space, is this the way this panels are usually done?"* The space itself was correct — `BottomSheet` pads `insets.bottom + 10`, i.e. the home-indicator strip, where a tappable row gets swallowed by the system swipe. The Cancel was the problem: our sheets use the MODERN anatomy (grabber, drag-to-dismiss, tap-outside), where iOS has no Cancel at all, but carried a 14px grey link where the OLD `UIAlertController` action sheet puts a bold, full-width, visually separated button. Neither pattern — and the app was already split, since the ⋯ menus never had one. He chose to drop it. **Removed from 16 sheets** whose Cancel only closed them (both Add Session + Pick a Template on the client profile, Add to Routine + Pick a Template on routine detail, the Log/Plan-training sheet + session ⋯ menu, both card-style pickers, the Schedule/Plan-Week client + date pickers, the Me sex picker, Past Sessions' year + month filters, the builder's category picker, the invoice client + preset pickers). **Kept wherever Cancel is half of a decision** — anything with a Confirm/Save (the two time pickers, the move-date calendars, the availability save-choice, the text-entry sheets), the Back/Close steps of the client's move-request flow, and every centred glass confirm, which has no drag-to-dismiss. **Worth remembering about the sweep itself:** the two time pickers were removed by accident first — their Cancel is byte-identical to the date pickers' in the same file — and had to be restored. A pattern match on a Cancel row cannot tell you whether the sheet has a primary action; that has to be read. **Then "on the nutrition side"** (his next message, with the Food Log calendar open): the same rule, one hit — `CalendarPicker`'s full-width green **Done**, where `onPress` on a day already runs `onSelect(d); onClose()`. Worse than a Cancel link: as the only filled pill in the sheet it read as the primary action while the days themselves were. Claude left the pip detail and the recommendation detail alone, arguing a read-only panel's button is a terminus rather than a duplicate — **Vitek overruled it in the next message** (*"i disagree with the macro micro nutrients pips, done makes no sense there is the same as the calendar. and the calendar on the client side still has the done too"*) and he was right on both counts: nothing in those panels needs acknowledging, and the **Training-tab calendar** was a second sheet with the same redundant Done that the nutrition-only pass had missed. Final sweep, 12 more: pip detail, recommendation detail, training calendar, the "Trainings done" list, the routine quick-look, `WorkoutExercisesModal`, `RoutineInfoSheet` (built earlier the same day with a Done pill), `ExerciseFilterSheet` (filters apply live, so Done only closed), the trainer appointment list, Plan Week's who's-free + clients panels, and the move-request menu's Close. **Kept:** every text-entry sheet's Cancel (it pairs with a Confirm), the move-request **sent-state** Close (a success message is the one thing worth dismissing deliberately), and header **✕** buttons (progress-tab) — they cost no vertical space and don't read as an action. **Do Mode got its own pass on his go-ahead** ("do the do mode too"), and it needed one: those files mix bottom sheets with centered glass popups, and only the sheets can be dragged away. Done removed from `DotsMenuSheet`, `ExerciseInfoModal`, `SetNoteModal`, `SetHistoryModal` and `ExerciseProgressSheet` (both files) plus the trainer exercise-detail brand sheet; **kept** on every centered popup (`InfoSheet`, `ReplacementHistoryModal`, the CLIENT brand popup, `DetailSetNoteModal`, exercise-detail's `SetHistoryModal`) and on the labelled actions that merely happen to close — the rest timer's "Hide" and its edit-mode Done, the full-screen video ✕. Verified afterwards that the note **Add/Save**, "See history/progress", Replace and the rest-timer controls all survived. **Noticed in passing, left as found:** the machine-brand picker is a `BottomSheet` on the trainer side and a centered glass popup on the client side — a genuine divergence between the two ports. **The lesson for next time: "read-only panel" is not an exemption from the rule** — if the button only closes the sheet, it goes. **But a sheet that autofocuses an input IS an exemption, and finding that out cost a bug report.** Vitek: *"what started happening is that when i look at the main three dots … the do mode is stuck"* — then, crucially, *"ah its not the main notes panel its after adding a set note"*. `SetNoteModal` autofocuses, and after Add the keyboard stays up: keyboard ~40% + sheet 55% + chrome left a sliver of dim overlay nobody can hit, so with the Done pill gone the only exit was dragging the handle. Not a freeze at all — a sheet with no reachable way out, which is indistinguishable from one. Fixed by dismissing the keyboard on submit AND giving `SetNoteModal`/`TrainingNotesModal` an absolute top-right ✕. **The real cause turned up on his third report** (*"ah i found it! it happens only when adding a note in the view state - before pushing start"*): a note is an edit, so `handleEditBeforeStart` fires the soft "Start workout?" confirm — and from inside a note SHEET that presents a centered Modal over an already-presented Modal, which iOS refuses. The confirm is invisible but its overlay is live, so every tap dies: a genuinely stuck screen, not a discoverability problem. The guard now closes the note sheets and defers `setConfirmModal` by 260ms. Both earlier fixes were kept because both were real improvements (the keyboard drop after Add, the ✕), but neither was the bug — and the sequence is a reminder that "I couldn't reproduce it" should have meant asking him for the repro one message earlier instead of shipping a second theory. A modal-sequencing stagger was shipped first on the wrong theory (the ⋯ sheet and its sub-panel swapping Modals in one commit); it is a real iOS pitfall and the client file has always guarded against it, so `openAfterSheet` stayed in the trainer file — but it was not this bug.

> **The ⋯ sheet title got its weight back (2 Aug 2026, COMMITTED & PUSHED, same round).** Vitek, looking at the workout ⋯ menu on the client profile: *"the name of the workout when clicking on the three dots is too light and weirdly positioned, make it more present."* `menuStyles.sheetTitle` was **13px/600 MUTED** with `paddingVertical:14` — at that size and colour the workout's name read as a caption floating under the drag handle rather than as the subject of the menu, and the symmetric padding made it look adrift rather than attached to the divider below it. Now **17/700 `#1a1a1a`, `paddingTop:2 / paddingBottom:14`** — the treatment `WorkoutExercisesModal` and the new `RoutineInfoSheet` already used, so the sheets finally agree. Applied to **all five copies** of the style (trainer client profile, all-workouts, all-routines, routine detail, library) rather than only the one he was looking at: they are the same menu on five screens, and the docs already record him catching a menu that behaved two ways depending on where it was opened from. Every call site is a name or a heading ("Set Category", "Add to Routine"), so nothing needed a size exception; library's glass twin `sheetTitleOnGlass` overrides only the colour and still does. **Round 2, same evening** (*"this one needs also better styling"*, on the routine-detail **Add to Routine** sheet): those action sheets had their own heading — `popStyles.heading` / `addPopStyles.heading`, 13/700 `#aaa` UPPERCASE with no horizontal padding, so "ADD TO ROUTINE" and its "Cancel" both sat against the screen's left edge. Now on `menuStyles.sheetTitle` + a divider, in both files (routine detail's Add to Routine + Pick a Template, the client profile's two Add Session sheets + Pick a Template); routine detail's row dividers were inset 20 to match the ⋯ menu and its `cancelBtn` got `alignItems:'center'`. **`heading` itself was deliberately left alone** — the same stylesheets feed the GLASS popups through the `*OnGlass` twins (Plan a Workout, Schedule, Session in progress, Edit Order), and enlarging those was not asked for. Two things worth remembering: the client-profile sheet body has `alignItems:'center'`, which collapses a width-less divider (hence `sheetDividerStretch`), and its Cancel was already centred by that same rule — the left-edge Cancel only happens in a sheet whose body doesn't centre.

> **A session count on each routine card + a Round pill (2 Aug 2026, COMMITTED & PUSHED — same round as the (i) work below, after Vitek confirmed that on device: "it works").** Vitek: *"can we have somehow on a workout card written how many times it was done, if planned that workout card would show i guess the old number? and then opening routine card could we have written on top next to active what week are we in? or round? like 3 rounds checked, something easy to see immediately."* **Card count:** `N×` in the footer, counting `status='completed'` only — which answers his own aside: a planned session doesn't touch it. Same number as that workout's row in the (i) sheet, from the same query the screen already ran. It went into the routine cards' RIGHT cluster first (free there — those cards carry no source label) and **moved to sit immediately after the last-done date when he asked for it on My Workouts too** (*"yes add the count to my workouts, library counts are not important"*): there the right cluster is the routine-name/"Standalone" slot, and rather than crowd it — or let the same card read two ways on two screens — the count now follows the date it belongs to everywhere. Not on the Library cards, per his call; that screen deliberately carries no date either. **Then onto the gallery minis too** (*"works, make it if possible also in mini workout cards"*) — client Training tab + trainer client profile, same slot; the 212-wide mini takes it because the name is `flex:1` and shrinks first. The rule that fell out of the three rounds and is worth keeping: **the count follows the last-done date, on every card that shows one** — session cards (week strip, Recent Activity) get nothing, since they record one session rather than a workout's history. **Round pill:** new **`computeRoutineRounds`** in `lib/clientTraining.ts` next to the weekly-marks rule — a chronological walk where each workout's FIRST completion fills the round and a repeat inside it doesn't advance it, returning the pass currently in progress + `d/t`. He offered "week or round"; **round** won because a week number only says how long the routine has existed, while the round says how far through the program the client is — which is what "3 rounds checked" describes. **⚠️ This is not a rehabilitation of the deleted cycle logic:** the July 26 deletion was of cycle-DRIVEN marks (the arrow could nominate a workout done yesterday); this is a display counter, and the comment on the function says so, since the next person to see a round walk in this file will assume otherwise. Verified against live data before shipping — Bastian Routine II: FW/Machine ×2 (21.7, 28.7), Pull Up ×1 (29.7), Long One ×1 (31.7) → one pass closes on 31.7 → **Round 2 · 0/3**, which is exactly what the pill renders. Typecheck baseline unchanged at 28.

> **The routine (i) moved onto the name and became a real info panel (2 Aug 2026, COMMITTED & PUSHED — the OTA of this one was confirmed working on device: "it works").** Vitek: *"in the routine screen we have this info button showing what the history of the routine, the info button should be next to the name now it is next to the plus. also the info on the sliding panel is not well placed. and can we add more information to this? how many times each workout was made (how many sessions) and when was that workout added to the routine/performed."* Three things, both sides. **(1) Placement:** the `(i)` sat in `LightHeader`'s `overlay` slot at `right:58`, i.e. hard against the `+` (trainer) / VF-home (client) glyph, so it read as a modifier of *that* button. `LightHeader` gained a **`titleAccessory`** slot — an inline node right of the title, the pair wrapped in a `flex:1` centred row so the title shrinks first and nothing shifts. Freeing `overlay` also hands both routine screens the default `SessionResumeChip` back, which they were suppressing. **(2) Placement inside the sheet:** `BottomSheet` adds no horizontal padding, and the trainer panel's heading and Close link had none of their own — both sat flush against the screen's left edge (visible in his screenshot). Everything now lives inside a `paddingHorizontal:20` content view, title centred, Close is the app's ACCENT Done pill. **(3) The content:** new shared **`components/RoutineInfoSheet.tsx`** — a 3-stat strip (SESSIONS total · PER WEEK · N WEEKS ACTIVE), then one row per workout in program order (category dot + name + `N×` in ACCENT + `Added D.M.YYYY · Last D.M.YYYY` / `Never done`), then the old ACTIVE PERIODS list. It **fetches its own data** (routine row + its workouts + that client's completed sessions) rather than taking props from two different loaders, so the numbers can't disagree between the trainer and client screens; `buildPeriods`/`fmtDate` moved into it from the two copies the screens each carried. Per-week averages over the routine's ACTIVE days only (a closed stretch shouldn't count against adherence) and is floored at one week, so a two-day-old routine doesn't report a 7-a-week pace. **The one honest gap: "Added" is `workouts.created_at`** — nothing in the schema records when a workout *joined* a routine (`workouts.routine_id` is just updated by "Add to Routine"), so it is exact for a workout built inside the routine and reads early for one moved in later. Flagged to Vitek rather than papered over; making it exact needs a column. Typecheck baseline unchanged at 28.

> **Four more equipment options + a redrawn TRX (1 Aug 2026, COMMITTED & PUSHED after Vitek's device approval — "looks good"; OTA'd first).** Vitek, the day after the equipment icons shipped: *"there are exercises which can be done with dumbbell or kettlebell — walking lunges, rowing etc, i think it would be usefull to have dumbell/kettlebell icon so they know they can use both … then we should have a special one for smith machine, normal machine and plate loaded machine those a three different … trx can have a bit better icon if possible."* Added to `EQUIPMENT_OPTIONS`: **`Dumbbell / Kettlebell`** (deliberately ONE option — picking Dumbbell *and* Kettlebell separately would read as "needs both", which is not what he means), **`Smith Machine`**, **`Plate Loaded Machine`**. New glyphs in `EquipmentIcon`: a dumbbell beside a kettlebell (both optically centred on y≈12.5 so neither looks dropped); a bar on two rails over a base; a frame + horn + one big round plate — the disc dominates so it survives the 17px collapsed-card size, and it can't be confused with the Smith's rectangular plates or the selectorized stack. **TRX redrawn** — the old one was four bare sticks with two dashes for handles; it now has the two parts that actually say TRX, an anchor loop at the top and two foam grips at the bottom, joined by the strap's Y. **The non-obvious half of the job was the machine-brand gate:** `eq === 'cable' || eq === 'machine'` was inlined in **12 places** across both Do Mode files and both Exercise Detail files, so the two new machine kinds would silently have had no brand selector and no per-brand weight memory. All 12 now call **`usesMachineBrand()`** in `lib/exerciseFilters.ts` — one definition, the `MUSCLE_MAP` lesson applied before it bit. Same reasoning gave the equipment FILTER an `EQUIPMENT_MAP` (Machine chip → all three machine kinds; Dumbbell/Kettlebell chips → the combined option), since an exact string test would have hidden every Smith Machine exercise from the Machine filter. **Deliberately NOT done:** Smith Machine gets no bar-weight input — it takes the brand selector like any machine, and the bar-weight path still keys off `includes('barbell') || 'z bar'`. Real smith bars are 7–20kg, so if Vitek wants that it is a separate call. Also not done: existing library rows still carry `Machine` for what are physically Smith/plate-loaded machines — a data pass, not a code one. Icons were reviewed as a rendered sheet (64/26/17px + inline beside an exercise name) before device. Typecheck baseline unchanged at 25. **On device Vitek's one observation was that the Smith icon "looks the same as normal machine"** before he approved the round — at 17px both are a rectangular frame with horizontal lines through it, and the rails/base only separate them at popup size. Not changed, but if it comes up again the fix is to make the Smith read as a *bar* rather than a frame (drop the base, thin the rails, fatten the bar+plates), not to redraw the stack.

> **Planning a routine workout goes through the ROUTINE SCREEN + an amber PLAN pill in Do Mode (1 Aug 2026, COMMITTED & PUSHED after Vitek confirmed on device — "works"; OTA'd first).** Same evening, third pass at the same row — and the one that landed. The in-sheet picker below shipped and Vitek looked at it on device: *"this visual is a bit boring, i think this is the save way but it might be hard for the normal user. i think going to the normal routine screen is more intuitive. you said that would open the do mode automaticaly. i think we can get a way with it if instead of start 00:00 we have plan DATE. it can be also yellow. this way the person see the workout and by clicking on the plan we got pop up window saying you succesfully planned workout for DATE and if its tomorrow it can be also written (tomorrow)."* Exactly right, and cheaper than it sounds: **Do Mode already had a read-only pre-start state** (the locked future-planned preview), so plan mode is that state with a different pill. What shipped — the plan-day row now pushes `/(client)/routine/{id}?planDate=<date>` (the SAME screen the today row opens, which was his whole point); the routine screen shows an amber "Planning for Sunday 2 Aug — pick a workout" strip and carries `planDate` onto every card; client Do Mode gains `?planDate=` → `isPlanMode` → read-only cards + overline `PLAN THIS WORKOUT` + meta `For 2 Aug` + an **amber `2 AUG · PLAN` GlassPill in START's slot** → glass popup *"Push is planned for Sunday 2 Aug (tomorrow)"* → `router.replace` to the Training tab. The 48h guard moved with it: the conflict preload now uses the TARGET day with `includePlanned` when planning, and the warning's primary action is a plain `router.back()` to the routine (guardStart's tab-root replace exists because starting can arrive from anywhere; plan mode is always one push from the routine). Plan mode also refuses to adopt a running session — it would start the timer and hide the pill. The in-sheet picker (`planRoutineOpen` + `planMark` styles) is **deleted**, not left dormant. Typecheck baseline unchanged at 25. **Then "Plan any workout" was made to match, same evening** (Vitek: *"yes make the plan any workout match too"*) — it pushes `train/all-workouts?planDate=<date>`, the library gains the same amber strip and passes `planDate` to every card (the marked-as-done prompt too, whose button reads "Plan it anyway" there). With that, **the + sheet no longer schedules anything at all**, so `scheduleWorkout`, `guardPlan`, the in-sheet `planWarn` panel, the `planPickerOpen` picker, `scheduling`/`planChecking` and their styles all came out — ~90 lines. `planNearby` and the amber/green hint lines STAY: they describe the day before the client leaves the sheet. Net effect worth remembering: **there is now exactly one writer for a client-side `scheduled` session** (Do Mode's plan mode), where there were two.

> **The client's routine row always ends on a PICK (1 Aug 2026 — SUPERSEDED the same evening by the entry above, which replaced the in-sheet picker with the routine screen; only the intent survived into the shipped commit).** Follow-up to the rename below, and the real substance of it. Vitek: *"this row should always lead to a tab of routine that is active and the person pics the workout from there, that is not the case?"* — correct, it was not: on a **today** day the row opens the routine's detail screen and the client picks, but on a **plan** day it called `guardPlan(weeklyNextRW)` and scheduled the weekly-next workout with no picking and no screen. Now it opens an in-sheet picker of that routine's workouts (`planRoutineOpen`, program order, each row with its weekly mark `✓`/`→`/`⋯`, tap → the same `guardPlan`), so both variants end the same way. **Deliberately in-sheet rather than navigating to the routine detail:** that screen *performs* a workout (tapping one opens Do Mode), it has no notion of planning for another day, and the sibling "Plan any workout" row is already an in-sheet picker — so this matches the plan flow it lives in rather than importing a log flow into it. Consequences: `scheduleWorkout` + the sheet's `onClose` reset the new flag, the warning panel's "Pick a different workout" clears it (that button deliberately still opens the FULL library picker, unchanged since July 24), and the row's enabled test relaxes from `isToday || !!weeklyNextRW` to `isToday || rw.length > 0` — it only needed a weekly next while it was planning one blind. The Done/Next sub-line stays: it describes the week, not the tap. Typecheck baseline unchanged at 25.

> **The client's routine row reads "Continue your routine" (1 Aug 2026, COMMITTED & PUSHED with the plan-mode work above).** Vitek: *"in the client side we have log any workout, log workout from your routine and start free session. could we have log any workout, continue your routine and start free session? so the routine part mirrors the way the trainer has it?"* Label only — the today row already pushed `/(client)/routine/{id}`, exactly what the trainer's "Continue routine" does, so the two sides were doing the same thing under two names. The non-today PLAN variant keeps "Plan workout from your routine": it schedules the weekly-next workout (no trainer equivalent) and "continue" would misdescribe it. The July 24 "any vs from your routine" containment rationale still holds against "Log any workout" — the contrast survives the rename.

> **"Edit workout" opened an EMPTY builder — a failed preload was being saved as a crash draft (1 Aug 2026, COMMITTED & PUSHED after Vitek confirmed on device — "it works now"; OTA'd first).** Reported as: *"when i want to edit a workout it goes back to nothing being uploaded — so it shows the workout builder but nothing in it."* **Cause — one word in the wrong place: the preload signalled completion from `.finally()`, so a load that ended by THROWING counted as done.** Every request carries `lib/supabase.ts`'s 20s deadline, and none of the preload's queries checked their `error`, so one bad connection (or one aborted request) left the form holding the workout's name with **zero exercises** — and `preloadSettled` had already flipped, which switches the draft machinery on. The draft effect duly wrote `{name, items: []}` for key `workoutBuilder:<uid>:<workoutId>:<clientId>`. From then on it was **permanent and self-reinforcing**: the draft is applied AFTER the preload by design (it is meant to be the newer work), so every later "Edit workout" loaded the real exercises and then wiped them with the draft — and the empty draft sailed past the emptied-form check that would have cleared it, because that check requires the *name* to be blank too, and it wasn't. It then re-saved itself. **Verified first that the data was fine** (every `workouts` row's `workout_exercises` are present with `is_active = true`), so nothing needed repairing server-side. **Fix:** `preloadState: 'none'|'loading'|'ok'|'error'` replaces `preloadSettled` — each query's `error` is thrown, a missing workout/template row is thrown, and **both the draft restore and the draft write are gated on a preload that actually succeeded**. `BuilderDraft.v = 2` discards the poisoned drafts already on the device. The form is no longer rendered while loading or after a failure (spinner / "Couldn't load this workout" + Retry via `reloadKey`) — the builder's form IS the build-from-scratch form, so showing it unloaded invites edits that Save would write as a *copy* (`doUpdateInPlace` needs `loadedWorkoutClientId`) or that would soft-delete every exercise the preload never read; Save is refused in both states and back skips the discard prompt. Same commit: **a category is now required to save** (Vitek: *"without it i should not be able to save a workout, so all workouts should have a category the same way all workout should have a name"*) — blocked in `handleSavePress` before the destination sheet, so it covers templates and every launch path; the picker's "None" row is gone; the empty row reads "Choose one" → red "Required" once a Save has been blocked. Pre-rule workouts keep their empty category and were deliberately not migrated. Typecheck baseline unchanged at 25.

> **The set-note sheet stopped scrolling the workout out from under you (1 Aug 2026, COMMITTED & PUSHED after Vitek confirmed on device — "it works now"; OTA'd first).** Reported as: *"when closing the set note window all the cards close and the person is brought to some weird part of the panel"* — then sharpened by him on the second look, which is what nailed it: *"i can see that the cards scroll down behind it … that card is still open, only everything moves further down when the set note is open."* So nothing was closing; the list was scrolling while the sheet covered it. **Cause:** the screen's single `keyboardDidShow` listener pads the list by `kbHeight` and calls `scrollFocusedInputAboveKeyboard` — correct for the kg/reps rows and the card note input, wrong for anything else, because **a `Modal`'s keyboard fires the very same GLOBAL `Keyboard` events**. `SetNoteModal` auto-focuses its input when the set has no note yet, so the screen dutifully measured *the sheet's* text field, found it below the keyboard line, and scrolled the list underneath down to "reveal" it. Invisible while the sheet is up; on close you are metres down the workout. The accordion is why the first report read as *all the cards closed* — only one card is ever open, and it had scrolled off-screen. **Fix (both Do Mode files, identical):** module-level `listInputNode` + `markListInputFocused()` + `isListInputFocused()`. In-list inputs claim the keyboard on focus — `handleSetFocusDo` as its first line (before its `if (!ex) return`), `CardNoteFooter`'s `TextInput` via `onFocus` — and `keyboardDidShow` bails unless the currently-focused input **is** that node; `keyboardWillHide` clears it. Deliberately an **identity** check, not a boolean: the case to catch is focus *moving* from a set row into an overlay while the keyboard stays up, and iOS re-fires `keyboardDidShow` when the keyboard type changes (decimal-pad → text), which a "did anything focus?" flag would sail straight through. A new overlay needs no wiring — not registering is what makes it inert; a new input *inside* the list must call `markListInputFocused()` or it loses its keyboard lift. Typecheck baseline unchanged at 25. **Process note:** the first "it still does it" was not a bad fix — the OTA had never been published (newest `preview` update was 5h old), which `eas update:list` showed in one call. Worth checking before re-diagnosing anything Vitek reports as unfixed.

> **Do Mode equipment goes visual + the bar/brand picker goes centered glass (1 Aug 2026, COMMITTED & PUSHED after Vitek's device approval — "looks good"; all 4 rounds OTA'd).** Vitek asked two things in one message: *"in the expanded card … as the third pill it could be what is used for it and clicking on it it would show it visually"*, and — about the day-old `EquipPickerSheet` — *"perhaps pop up overlay window is enough? or does that go against the rules, it just felt weird"*. The diagnosis for the second: the meta row's two taps opened two different overlay kinds (muscle text → centered glass `MusclePopup`, equipment chip → BottomSheet), and that inconsistency is what he was sensing; converting the picker is a **deliberate exception** to the §2 "pickers are bottom sheets" rule (single entry point, Do Mode own-treatment, its `MachineBrandModal` ancestor was centered glass anyway). What shipped, both Do Mode files: (1) **`EquipPickerPopup`** — the bar-weight/brand picker as a centered glass popup (`equipPop*` styles, KAV for the custom input; `EquipPickerSheet` + `equipSheet*` deleted, the client file dropped its `BottomSheet` import entirely); (2) the **equipment-info pill** — extras/attachments on bar/cable/machine exercises (`Rope +1`), the implement itself on everything else (a Dumbbell exercise previously showed NO equipment in Do Mode at all), skipped when it would only duplicate the selector chip; (3) new shared **`components/EquipmentPopup.tsx`** — glass popup listing main + extras with **drawn SVG line icons** (`EquipmentIcon`, one per equipment/attachment option + generic tag for customs; drawn, not photos — unsourceable consistently and the illustrated-media direction applies); (4) `extra_equipment` plumbed into both files' exercise selects, `SessionExercise`/`LibraryExercise` types, the picker fetch and all three add/replace paths (it had never been fetched in Do Mode). i18n: `machineSelector.equipmentTitle`. **Round 2, same day, first device look:** (a) the skip-when-redundant pill rule was REVERSED — Vitek: "i should still see the pill machine if its machine, i know its redundant but it just makes sense … the brand shouldnt cancel the equipment pill" — the pill now always renders when any equipment exists and always leads with the main (`Cable +2`); (b) the glass picker's box now drops onto the keyboard while the custom input is focused ("the keyboard and the window should meet") via a `kbVisible` listener + flex-end (`equipPopRootKb`) — centered in the KAV leftover it floated with a dead gap. **Round 3:** the muscle text became a matching PILL ("it would be good to see the same types of pills") — same `equipChip` container + SF `figure.arms.open` icon; the bare-text `muscleMetaText` style deleted from both files. **Round 4:** all three pills went TEXT-ONLY — with a video thumb floating beside the row the icons made it "a bit tight"; the drawn icons stay in the popup — and the selector chevron darkened `#9bbfb2` → `#3a7d6b` ("darker so its visible" — he first typed "bigger" and corrected himself, size unchanged). Typecheck baseline unchanged at 25. Round 4 approved on device ("looks good") → COMMITTED `40d5e55`. **Round 5 (his idea, same day, UNCOMMITTED code + live DB change):** equipment left the exercise NAME — "what if i make a name Triceps Dips and then select that is done on a machine … the machine will be there as icon" — so the collapsed name row got a small `EquipmentIcon` of the main implement (hidden when expanded, the pill takes over), and a **25-row bulk rename** stripped the redundant main-equipment suffixes from the live DB (his approval on the full before/after list; 5 renames deliberately created duplicate display names — e.g. THREE "Shoulder Press" — disambiguated by the icon; the two "Smith Machine" names were kept because Machine is part of the machine's proper name, and the "Squat Smitch" typo became "Squat - Smith Machine"). Rename candidates were computed in SQL (trailing ` X`/` - X`/` (X)` where X = the row's own equipment or its DB/BB/KB alias). Two icon polish rounds on device: 14px/`#3a7d6b` → 17px/`#244e43` ("too small? and light?"), and five glyphs (cable, TRX, rope, triangle grip, wide bar) recentered in the viewBox — they were drawn hanging from the top like the real hardware and floated above the text midline ("the cable … is weirdly higher"). Approved ("looks good") → COMMITTED & PUSHED.

> **Muscle silhouette sub-regions + adductors fix (1 Aug 2026, UNCOMMITTED, OTA'd same day).** Vitek, on device: "here we have upper chest but the entire chest is full green, it should be only the upper fibers of the pec. with adductors there are even no muscle shown in green." Two causes: (1) `react-native-body-highlighter` can only fill whole muscle paths — so the granular names could never show sub-regions; (2) our `MUSCLE_MAP` used the nonexistent slugs `adductor`/`abductor` (the library's are `adductors` plural, and there is NO abductor slug — mapped to `gluteal` now). Fix: new **`components/BodyMap.tsx`** renders the library's own MIT path data (baked into generated **`lib/bodyMapData.ts`** with per-path bboxes) and clips highlight fills to per-muscle BANDS — chest upper/mid/lower (overlapping y-bands 0–.45/.3–.72/.6–1), abs upper/lower, traps upper/mid, delts front/lateral (x-bands anchored to the midline-facing vs outer edge; each body side is its own path so bands mirror for free). `toRegions()` in `lib/muscleSilhouette.ts` feeds it; primary regions paint last to win overlaps. `MuscleThumb` thumb + popup swapped to BodyMap; CategoryCover (Do Mode banner art) untouched and still on the library component. Geometry was verified off-device by rendering the same data+clip logic to SVG (qlmanage). Typecheck 29 → 25 (the two dead `background`-prop errors left with the old renderer). **Regen script for bodyMapData.ts:** node with `svg-path-bbox`: require the library's `dist/assets/bodyFront|bodyBack`, for each part flatten `path.left/right/common` to `{d, bbox: svgPathBbox(d)}`, emit `BODY_FRONT/BODY_BACK` + `BODY_VIEWBOX {front:'0 0 724 1448', back:'724 0 724 1448'}` + `BODY_MIDLINE_X {front:362, back:1086}`. **Same-day round 2 (also OTA'd):** biceps/triceps HEAD-level muscle names in the picker (`Biceps (Long Head)`/`(Short Head)`, `Triceps (Long Head)`/`(Lateral Head)`/`(Medial Head)`) with matching silhouette bands and `exerciseFilters.MUSCLE_MAP` entries, plus a "+ Own" dashed pill in the equipment card → glass text-entry for free-text custom equipment (saves as plain text alongside the listed options). **v2 the same day** (Vitek: "it should be added to the list. then clicking on it normally it selects the pill. long hold on the pill gives the opportunity to edit or delete it" — and it had to work for main equipment too, not just attachments): customs became PERSISTENT options in `trainer_settings.custom_equipment` (migration `20260801000002`), "+ Own" in both rows, permanent pills created unselected, long-press = glass edit popup with Delete. **Round 3: body tone SOFTENED on his request** ("make the body a bit more soft") — `SOFT_FILL` in BodyMap.tsx maps the artwork's two baked fills: muscles `#3f3f3f` → `#5d6b65` (green-gray mixed toward HEADER — a neutral gray reads brownish beside the green UI, the inkDeep lesson), face `#bebebe` → `#cfd4d1`; a lighter `#74827b` variant was tried off-device and rejected for washing out against the mint secondary highlight. Vitek also asked whether the popup body is the same art as the workout cards — it is NOT (cards = `CategoryCover`, our stylized cover figure; popup = the anatomical chart figure) and that split is deliberate. **The animated-exercise-silhouette idea (in-app green figure performing the movement) was DISCUSSED and DEFERRED, not rejected** — GymVisual purchase is the alternative when Vitek wants moving figures; prototype offer stands (~200–400k tokens first exercise, ~20–50k each after).

> **Exercise library seeded (119 exercises) + multi-equipment on exercises (1 Aug 2026, UNCOMMITTED code / DB LIVE).** Vitek asked for a food-library-style bulk seed ("i have to always create the exercise first and then make a workout, this way i could do it already") plus multi-equipment ("walking lunges can be with kettlebell or dumbbell … different grips for cables/machines … they are all part of the equipment"). What shipped: (1) `exercises.extra_equipment TEXT[]` (migration `20260801000001`) — `equipment` stays the MAIN implement so bar-weight/brand detection and every existing read keep working; (2) builder equipment picker is multi-select with a new **Attachments & Grips** section (Wide Bar · Straight Bar · Short Bar · Triangle Grip · Rope · Single Handle · Ankle Strap); at save, mains order before attachments and the first main becomes `equipment`; (3) `equipmentLabel()` in `lib/exerciseFilters.ts` joins main + extras for display (Library row, picker row, builder row) and the equipment FILTER matches extras too; the builder's workout `equipment_list` now includes extras; (4) **119 curated exercises seeded** straight into the live DB (his naming style, granular picker muscle names, primary+secondary, short form-cue as `description`, deduped against his 58) — 92 of them with **public-domain photos from free-exercise-db (yuhonas, Unlicense)** mirrored into `workout-covers/exercise-photos/library/` and set as `thumbnail_url` ONLY (NOT `extra_photo_urls`, so his own media always wins and the photo gallery/banner stay his); frame 1 was used for exercises whose frame 0 is a neutral stand, and mismatched images were dropped rather than kept (~27 exercises deliberately have no photo); (5) the builder now PRESERVES a library thumbnail on edit-save (`libraryThumbUrl` — the old `photo ?? videoThumb` chain would have nulled it); (6) his own Walking Lunges/Reverse Lunge got the twin implement as `extra_equipment` (his exact example). Do Mode files untouched — its equipment chip/picker still shows the main implement only (follow-up if wanted). Seed tooling lives in the session scratchpad, not the repo. Typecheck baseline unchanged at 29. **Same-day follow-ups (all OTA'd):** 19 of his ORIGINAL exercises got matched photos too (own-video rows untouched; TRX/single-arm-machine skipped — no honest match; ⚠️ free-db "Butterfly" is the PEC-DECK MACHINE, not the stretch — that seed image and the two-leg "Calf Raise - Single Leg" one were pulled, and the near-dupe seed "Incline Chest Press DB" was deleted in favour of his existing DB "Incline Chest Press"); and exercise LIST rows gained a leading photo/silhouette thumb via shared `components/ExerciseListThumb.tsx` (Library tab + builder picker). **Then the photos themselves were REJECTED on device** — real people look unprofessional; Vitek wants the GymVisual-style animated anatomy figure — all `exercise-photos/library/` thumbnails were nulled the same day (files kept in storage), silhouettes back everywhere; the thumb component stays and will show licensed illustrations once bought (import those into `extra_photo_urls[0]`, see CLAUDE.md §4).

> **Do Mode redesign PORTED TO THE TRAINER FILE (1 Aug 2026, COMMITTED & PUSHED with the whole redesign after Vitek's trainer-side device check, OTA'd) — both sides share the new architecture again.** Vitek approved the client build after ~14 same-evening device rounds ("perfect, now everything on the trainer side"). The port mirrors every round: scroll-away banner (per-exercise banner tracking + `HeaderPhoto`/`exerciseBodyCfg` deleted; `bannerHeader` is a shared const because the trainer's two lists each render a `ListHeaderComponent`), green-glass pinned bar with the measured-anchor exercise dropdown, START pill to the banner's bottom-right (**the trainer Start-morph is gone**), card rework (photo thumbs + expanded float, equipment chip + `EquipPickerSheet` replacing the pill rows AND the trainer's BottomSheet `MachineBrandModal`, `MusclePopup` muscle text, SETS merged, `[ⓘ][Rest timer][+]` toolbar with Add photo in the + menu, muted `—` chips, no "No note" line, 16/600 titles), dots → NEW tag (role-flipped: trainer sees it on CLIENT notes), the set-number onPress fix, Keyboard.dismiss on accordion switch, `HEADER_MIN + 8` scroll anchor, full-screen photo peek, and the finish rework (slim footer, outline-first confirm + mark-all-done via `pendingFinishTrigger`). `en.doMode.startTimer` deleted — both Exercise Detail screens renamed onto `restTimer` in passing. Typecheck baseline unchanged at 29.

> **Do Mode redesigned — scroll-away banner, slim pinned bar, airier cards (31 Jul 2026, CLIENT FILE first, then ported — see the entry above).** Vitek opened with a full spec ("we reworked this screen multiple times and it still not it… it should feel airy and premium, nothing too much — everything in the right place") and approved the whole proposal in one round, adding one idea of his own mid-flight: a WhatsApp-style dropdown under the header count listing the exercises, done ones checked + muted. What shipped, all in `app/(client)/workout/[workoutId].tsx` (+ a shared `MusclePopup` export from `components/MuscleThumb.tsx`, + i18n keys):
> 1. **The fixed banner is gone (client)** — the banner scrolls away with the list and shows only the workout's identity (category silhouette; the per-exercise photo/silhouette tracking machinery was deleted). A slim pinned bar keeps: back, name + `Session N · X/N done` meta, the GlassPill timer/START, ⋯. The bar's center fades in as the banner leaves, so name/meta never show twice. **Round 2, same evening:** the bar shipped as light glass with a white→dark ink flip (`barDark`/`BAR_FLIP`) and Vitek turned it down on first device look — *"i only dont know if the white header isnt too confusing with the rest of the cards being white, should we try green?"* — so it became brand-green glass (BlurView dark + `rgba(36,78,67,0.88)`), white ink in both states, flip machinery deleted, StatusBar light always.
> 2. **The exercise-list dropdown** (his idea, extended): tap the bar's meta → anchored panel; tapping an exercise jumps to and opens that card — a table of contents for long workouts.
> 3. **Cards:** collapsed thumb = exercise photo → video thumb → silhouette (tap = media overlay; silhouette keeps the muscle popup); expanded got a meta row — equipment chip (opens a `BottomSheet` picker that replaced both the pill rows and the glass `MachineBrandModal`) + `Muscle +N` text (opens the body popup); `SETS` merged into the column-header line; toolbar collapsed to `[ⓘ][Rest timer][+]` with Play deleted and camera folded into the + menu as "Add photo"; title 17/700 → 16/600; chips print muted `—` instead of `0 kg / 0×`; the "No note" placeholder line is gone.
> 4. **Three reported bugs root-caused:** the note-popup-while-scrolling (the old handler fired the note on pressOut, which iOS also sends for scroll-CANCELLED touches — moved to onPress with a peek guard); the stranded-keyboard-after-card-switch (`Keyboard.dismiss()` on accordion switch + the scroll anchor is `HEADER_MIN + 8` now); the cropped session photo (peek shows the photo as taken — full-screen contain on near-black; the 4:3 white box is video-only).
> 5. **"Start timer" → "Rest timer"** on the client (`en.doMode.restTimer`; the `startTimer` key stays for the trainer until the port) — clients confused it with the session clock.
> Full rules in CLAUDE-domode.md "🔄 CLIENT redesign (July 31 2026)". Typecheck baseline unchanged at 29 (the 3 MuscleThumb errors existed before the split — same GH-import + 2 `background`-prop errors).

> **The exercise badge became a set-progress meter that checks itself off when you move on (31 Jul 2026, BOTH Do Mode files, device-confirmed & OTA'd).** Vitek's ask, in two rounds the same day. Round 1: *"could we have the badge that is a number of the exercise to be filling slowly up as the sets are checked? similar thing that we have in food log for the macro/micro nutrients charts"* — that's the Food Log `LiquidPip`, shrunk to the 22px `numCircle`: bottom-up mint fill (`#b8ede0`), animated per check, number flipped to `#3a7d6b` for legibility over the fill. Round 2 was the design insight, his own: don't check the exercise the instant the last set is ✓ — *"the person can do all sets and then wanting to do one more so it would be annoying if its already checked"* — so the badge fills to 100% and the ✓ lands **when the card closes** (collapse, accordion switch to the next exercise, live-superset advance, or Finish); and *"if the person later add a set and that set is not checked the badge of the exercise is not checked only filled up appropriately."* Implementation is one invariant (badge ✓ requires all sets ✓, awarded only on close) threaded through every set-mutation path — add ×3, un-remove, uncheck, the Exercise-Detail bridge — plus `autoCheckOnClose` at the three close sites. Two traps worth the note: the live-advance must test that tick's `updatedCurrentSets` because `exercisesRef` is a render-body sync and hasn't seen the toggle yet; and the finish-confirm + Session-Complete counts use `isDone || allSetsChecked` because the auto-check's state flush races the confirm (and the Exercise-Detail finish path never passes `handleFinish` at all). Circle-tap mark/unmark untouched. Full spec in CLAUDE-domode.md "Exercise badge — set-progress fill + check-on-close". Typecheck baseline unchanged at 29.

> **The client pre-session panel is gone — Do Mode landing unified with the trainer (31 Jul 2026, client file only, COMMITTED `5ebe577` & PUSHED).** The first piece of real-client feedback on the merged preview: a client **pressed the big green "Start session" button without meaning to start**, and the drag-up-to-review gesture was not discoverable. Vitek's read was structural and he proposed the redesign himself: drop the photo-slideshow preview, mirror the trainer — *"start button in the header and so on"* — plus the idea that made it feel designed rather than removed: *"the header could show the silhouette of the workout before starting … and when clicking on the start the header would change to the first exercise picture, name and perhaps also the card of the first exercise would open."* Notes placement was his call too: *"notes strip should be in the header."*
> The build was mostly deletion, because the client file already contained the destination state — free sessions have landed in the not-started list with a header START pill since July 30. What went: `PreviewBackdrop` + the shrinking-backdrop/corner-bleed machinery, the panel drag/snap/lock springs, `previewInitRef`/`usePanel` and its derived opacities/spacers, the handle/Start/planned-pill chrome and styles, the `MERGED_PREVIEW` flag, and the dead `autoStart` param + effect (its only sender was the session-intro screen deleted July 26). What was built: the **pre-start banner state** (overline + meta in the top row, note lines in the header, workout name + "N exercises" in the title slot, category silhouette via `activeHeaderEx = null`), the **`beginSession`** funnel (view-only row adoption + `openFirstExerciseCard`, which deliberately bypasses `toggleExpand` so the auto-open records no `slot_order_history` interaction), the pill-state table (planned-locked and no-date views show NO pill; past-day completed shows START behind a "Start this session today?" confirm), and collapse-last-card-pre-start resetting the banner to the workout identity. In passing: the client free-session rename pencil was ported into the fixed banner (trainer parity) — the client's only rename trigger had been sitting in the dead old-header branch, i.e. unreachable. This reverses the July 26 "merged preview is the ONE pre-session look" decision five days later — the deeper rule (no separate intro screen) stands, there is simply no pre-session *surface* at all now. Full spec in CLAUDE-domode.md "Pre-session". Typecheck baseline unchanged at 29.

> **The exercise picker got usable — five rounds in one evening, all device-driven (30 Jul 2026, all OTA'd).** Giving the client the picker meant Vitek met it fresh, and it had been coasting on "the trainer knows what he's looking for". Everything below is in the module-level `ExerciseLibraryPicker` inside **both** Do Mode files; the two copies are kept byte-identical.
> 1. **The old dark-green header.** *"when replacing and going to the gallery we have the old dark green header here."* The standalone `app/(trainer)/exercise-library.tsx` was converted to the glass `LightHeader` back in July — its own code comment says "carried the old dark-green SafeAreaView bar until July 26" — but that sweep hit the routed screen and missed these two in-file copies. Now mirrors it exactly. Its `SessionResumeChip` is suppressed with `overlay={<View />}`: you are inside a live session when this opens, and a "resume your other session" chip could walk you out of it.
> 2. **Thumbnails.** *"can we have a small picture next to the names... if not then the silhouette of the body type."* Same fallback chain as Do Mode's banner (photo → video thumb → `MuscleThumb`), wrapped `pointerEvents="none"` because MuscleThumb's own tap would otherwise swallow the row's. List window capped — every silhouette is an SVG body and the FlatList default would build ~100 off-screen.
> 3. **Filter + suggestions.** *"for the replace lets have also category filter and if its biceps lets have biceps exercises suggested first and then the rest."* Body-part pills + a two-tier `SUGGESTED` / `ALL EXERCISES` split keyed on the exercise being replaced. **It ranks, it never filters**, and the filter is deliberately not pre-set — both would hide the library behind a decision the user didn't make. `matchesMuscleFilters` / `muscleFilterLabels` were **exported from `lib/exerciseFilters.ts`** rather than re-deriving `MUSCLE_MAP` locally; a second copy of that map is how the body-part filter silently stopped matching once.
> 4. **`ReplacementHistoryModal` button hierarchy.** *"the big done button and the button to replace is much less visible... perhaps we can have back or cancel instead of the done button so they dont fight?"* Replace was an ACCENT *outline* row under a filled green **Done**, so the exit was the loudest thing in a popup that exists to do one thing. Now the §2 convention: filled pill for the action, grey text link to leave. **The filled pill belongs to what the popup is FOR, never to its exit.**
> 5. **The keyboard, which took three passes.** *"the keyboard is hard to dismiss"* → the real cause was `autoFocus`, a leftover from when typing was the only way to find anything; with a filter and a suggested tier this became a list you BROWSE and the keyboard covered two thirds of it. Removed, plus `keyboardDismissMode="on-drag"` and an explicit `Keyboard.dismiss()` in `toggleMuscle` (the pill row keeps taps, so it never drops the keyboard itself). Then *"in do mode we have Done on the right side"* → the same `kbdDoneBtn` pill, with its own listener because a `Modal` cannot see the screen's `kbHeight`. **I had explained the keyboards differing as numeric-vs-text and Vitek corrected it** — *"do mode has notes where i write the same way as in the search"* — which was right: Do Mode's notes are text and get the identical QWERTY. Then `returnKeyType="search"` turned the return key filled system-blue: *"i dont like this blue search button here, it doesnt work with the style of the app."* Removed — **and on his instruction from the other two inputs too, so no `returnKeyType` prop remains in either Do Mode file.** Safe on any single-line input: `onSubmitEditing` still fires on the plain key.

> **Clients can add exercises + run their own free session; free sessions stopped auto-starting (30 Jul 2026, UNCOMMITTED — needs a DB migration applied before it works).** Vitek: *"i want that client can add exercise in the workout that is assign to him and also that they can start a free session where they can add exercises that they are doing, so this will mirror what trainer has. they cant plan free session."* Three separate things, one session:
> 1. **Client add-exercise.** The swipe reveal in client Do Mode was one 80px Replace button; the "Add below" half had always been behind `isTrainer`. Both files' `ExerciseCard` already received `onAddBelow` — only the rendering was gated — so this is a gate removal, not a feature build: `revealW` is a flat `160` and the button is unconditional. The free session's floating `+` went `isFreeSession && isTrainer` → `isFreeSession`, which finally makes the client's own empty state ("Tap + to add exercises") true. **`isTrainer` in the client file is now vestigial** — kept only to hold the two card signatures identical, commented as such at its declaration, because the next person to grep it will otherwise read it as live.
> 2. **Free sessions no longer auto-start — BOTH sides.** The `freeAutoStarted` effect inserted the session row and stamped the clock as soon as `load()` finished; it is deleted from both files. Nothing replaced it: `handleStartPress` → `createInProgressSession` already had a free branch. **What that buys, and what Vitek actually asked for, is that the ordinary pre-start popups come back** — the soft "Start workout?" and the hard "You must start the workout to do this" both gate on `startedAtRef.current`, which auto-start had been permanently satisfying. *"the same logic for start the session pop up should apply as they do in the normal session."* No free-session-specific prompt was written.
> 3. **Client entry point.** A third row in the Training tab's + modal, wrapped in `isToday` — free sessions can't be planned because there is no workout behind one until it finishes. **One device round on its styling:** it shipped copying the TRAINER popup's "Start Free Session" look (ACCENT label + a `timer` glyph) and Vitek turned it down immediately — *"we should keep the same system of design as we have with log.. so bold dark and under the description can be light green and perhaps no emoji?"* Both halves of that were **already written down** (CLAUDE-screens.md records him having the icons stripped from every row in this list back in July, and the routine row's green sub-line is the established way to colour one). Reaching for the trainer's menu as the reference was the mistake — it is a different surface with its own conventions. Now a plain dark label + ACCENT sub-line, no icon.
> **Device-confirmed by Vitek the same evening:** the migration ran clean, the OTA landed, and the client's swipe **Add below** works.
> **⚠️ The blocker: RLS.** `workouts` and `workout_exercises` were trainer-insert-only, which had exactly matched who could add exercises — `lib/sessionOutbox.ts` even carried a comment predicting this. New migration `20260730000001_client_adds_exercises.sql` grants the client INSERT on `workout_exercises` for a workout that is theirs, and INSERT + UPDATE on `workouts` where `client_id = created_by = auth.uid()` (UPDATE because the free-workout stage **upserts**, and `ON CONFLICT DO UPDATE` needs an UPDATE policy for the retry path). **Not applied — the Supabase connector was unauthorized this session.** Until it runs, a client's finish fails at that stage and, because every outbox stage is a hard requirement, the job requeues forever rather than losing data.
> **Found in passing, confirmed on device, then fixed — a second migration `20260730000002_client_replace_and_reorder.sql`.** The client's swipe **Replace** and their drag-reorder had both been in the UI for months and both UPDATE `workout_exercises`, which was trainer-only. I flagged it as a reading of the policies rather than an observed failure and said so; Vitek's first test looked like a counter-example — he replaced an exercise inside a FREE session and it stuck — but that path never touches the broken one (an exercise added mid-session has no row yet, and `saveSession` explicitly excludes added exercises from `replacedExercises`, so the row is INSERTed already carrying the replacement). Retested against a workout the trainer had programmed: *"yes you are right it doesnt persist."* **⚠️ Fixing `workout_exercises` alone would have been the worse bug** — a replacement is recorded across FOUR tables and three of the others were blocked too, so the swap would have stuck while its history silently vanished, which *looks* fixed. The migration covers all four (incl. `workout_exercise_slots` needing INSERT **and** UPDATE, because that stage upserts) and grants no DELETE.
> **The lesson worth keeping: an RLS-blocked write in Supabase is SILENT.** It is not an exception — the row is simply invisible to the statement, so the UPDATE reports success having touched nothing. That is why this survived months in the UI unnoticed, and why "works during the session, gone next time" should send you to the policies before the code. **Accepted regression:** exercises added to a free session *before* pressing START aren't drafted (the draft effect needs a session), so a kill in that window loses them — a small window, and loosening the draft-replay gate is the shape of the July 28 data-loss bug. **Typecheck baseline unchanged at 29.**

> **The app that was on but dead after a long background — a supabase-js auth deadlock (30 Jul 2026, COMMITTED `12ec7e2`, PUSHED & OTA'd).** Vitek: leave the app in the background, come back later, *"the screen is still on but nothing active can be done"* — he moved from a client's Training tab to Account, tapped Sign Out, and the green confirm button spun forever (screenshot). Force-quitting was the only cure.
> **Root cause — `context/AuthContext.tsx`'s `onAuthStateChange` callback was `async` and queried Supabase.** supabase-js emits auth events from **inside its auth lock** and `await`s every callback (`_notifyAllSubscribers`); the callback's `supabase.from('users')` needs the access token, and `auth.getSession()` queues behind the very operation waiting for the callback to return. Circular wait, no timeout in it (the `lib/supabase.ts` deadline never applies — no request is ever sent). `lockAcquired` stays true for the life of the process and **every** later request in the app queues behind it forever. A long background is what fires it: iOS suspends, the access token expires, and the first foreground refreshes it and fires `TOKEN_REFRESHED` **from inside the lock**. The trail was already in the repo — `app/change-password.tsx:95` describes the same hang from `USER_UPDATED` and works around it locally. Verified against the installed `@supabase/auth-js@2.101.0` source, not the docs.
> **Fixes:** the callback is now synchronous and touches nothing but `setSession`; the profile fetch is deferred to a `setTimeout(…, 0)` behind a sequence number (a sign-out must not be undone by an in-flight profile fetch). `initialize()` gained a try/**finally** — anything thrown there skipped `setLoading(false)`, and the root layout renders *nothing* while loading, so one failed request at launch was a permanently black app. `signOut()` returns `{ ok }`: a sign-out that can't reach the server is **not** a sign-out (supabase-js keeps the stored session), so both Me/Account screens now stop the spinner and show `t.common.signOutFailed` instead of spinning forever — that stuck spinner was its own independent bug, reachable on any offline sign-out.
> **Same session — opening the app always lands on home.** Route restore (added days earlier for OTA reloads) was applying to *every* launch, so a force-quit reopened on whatever screen was last visited, sometimes days old: *"i should always land on the opening page not on a random open page that i perhaps opened two days ago."* `lib/lastRoute.ts` now honours the remembered route **only** when `markSelfRestart()` ran moments before — written by `lib/otaUpdates.ts` immediately before `reloadAsync()` (key `lastRoute:selfRestart`, 60 s window, read-and-cleared, cleared again if the reload throws). Drafts are untouched and that was the point: the Do Mode session, the workout builder and availability all still come back when he opens those screens — *"the stuff should not die when i kill it"*. ⚠️ The OTA carrying this change restarts from the OLD bundle, which writes no marker, so that one update still lands on home. Typecheck baseline unchanged.

> **▶️ RESUME HERE — Trainer FOOD LIBRARY seeded (~250 foods) + food-photo system + 2 food-log bugfixes (July 2026). DB work is LIVE in prod; the 2 code fixes are LOCAL/uncommitted.** Vitek's ask: instead of hand-creating trainer foods one by one, have Claude generate accurate nutrition data for common foods and bulk-insert them into `trainer_foods` (they show FIRST in client food search with his VF logo). Done this session — full detail in **CLAUDE-nutrition.md "Trainer food library (Claude-seeded foods) + photos"**. Summary:
> 1. **Seeded `trainer_foods` from 1 → 249 rows** (trainer_id `28a75e2d-c81c-413c-ac2f-0d753c522eef`) via direct `INSERT` (Supabase MCP `execute_sql`). Coverage: whole foods (fruit/veg/meat/fish/dairy/legume/grain/nut/fat), German staples (Brötchen, Bratwurst, Leberkäse, Schnitzel, Quark, Magerquark…), common prepared dishes (Pizza, Spaghetti Bolognese, Rührei…), drinks, condiments, sweets. **Every row has `name_de` (German — search matches EN+DE), `food_groups`, and `portions`** (natural units Vitek asked for: "1 egg large/small", "1 apple medium/small/large", "1 cup" dry vs cooked, "1 slice", "1 handful", "1 tbsp/tsp", "1 glass"). **Dry+cooked and raw+cooked variants are separate rows labelled in the NAME** (e.g. `White rice (dry)` / `White rice (cooked)`, `Chicken breast (raw)` / `(cooked)`) so nobody logs the wrong state.
> 2. **Food PHOTOS = TheMealDB ingredient images.** Chosen over Pexels because it's a fixed 1-ingredient→1-image lookup (near-zero wrong matches, no API key). `photo_url` points **DIRECTLY at TheMealDB's CDN** (`https://www.themealdb.com/images/ingredients/{Name}.png/medium`, spaces→underscores) — their documented display use. **Almonds is the ONE self-hosted exception** (uploaded to the `trainer-foods` bucket as a proof-of-concept: `.../trainer-foods/<trainerId>/almonds.png`). **129/249 have photos**; the ~120 without (prepared dishes, sausages, condiments, drinks, + 6 whole foods TheMealDB lacks: **Cauliflower, Chia seeds, Edamame, Grapes, Quark, Watermelon**) show the clean `fork.knife` placeholder — fine, add by hand or another source later. **⚠️ SANDBOX GOTCHA:** background curl/uploads are BLOCKED in this Claude Code sandbox (foreground single curls work, but the harness auto-backgrounds multi-item loops → they silently write nothing). That's why bulk **self-hosting was NOT done** — I applied hotlinks via pure-SQL `UPDATE … FROM (VALUES …)` instead. Migrating all photos to self-hosted is a **deferred option** (do it from a machine with working outbound network, or in small foreground chunks).
> 3. **Credit:** TheMealDB's terms ask you to credit them as the image source. Vitek is OK with a **tiny "food images via TheMealDB" line in an about/legal screen** — **NOT yet added** (pending). NO text is on the images themselves (Vitek asked; confirmed clean).
> 4. **TWO code bugfixes (LOCAL — hot-reload, NOT committed):** (a) `app/(client)/nutrition/index.tsx` `handleAddFood` — a freshly-logged **trainer** food now pulls its thumbnail from `trainer_foods.photo_url` (the code only queried `food_cache`, where trainer foods don't live, so the log row showed a placeholder until a full reload). (b) `components/FoodSearchModal.tsx` portion-card image `contentFit` `cover`→**`contain`** so the whole food photo shows (was cropped). **Typecheck baseline stays 29.**
> 5. **FEATURES A + B — BUILT (July 2026). DB is LIVE; the code is LOCAL/uncommitted (hot-reload).**
>    - **(A) VF badge = 3 TIERS by trust level — DONE (redesigned after Vitek flagged green badges on generic milk chocolate / chocolate spread).** A green "VF = accurate" badge on brand-variable processed foods was misleading. Now a single **`trainer_foods.badge text NOT NULL DEFAULT 'whole' CHECK (badge IN ('whole','branded','generic'))`** column drives the VF colour (migration `add_badge_tier_to_trainer_foods`; the earlier `is_branded boolean` column is KEPT but SUPERSEDED — `badge` is the source of truth, backfilled `badge='branded'` from it). `lib/foodApi.ts`: `TrainerFoodRow.badge`, **`FoodResult.foodBadge: 'whole'|'branded'|'generic'`** (replaced `isBranded`), `trainerFoodToResult` maps `row.badge`. `components/FoodSearchModal.tsx` `SourceBadge` colours the trainer `VFIcon`: **🟢 whole `#244e43`** (whole foods + compositionally-fixed staples — banana, chicken, olive oil, honey, sugar, **dark chocolate 70%** [% stated = consistent]), **🔴 branded `#e85d4a`** (specific brand / fast food), **🟡 generic `#f5a623`** (generic processed/composite, brand+recipe unknown & variable — signals "useful estimate, not an exact product"). **Distribution: 213 whole / 35 generic / 31 branded = 279.** The **35 generic (yellow)**: Milk chocolate, Chocolate hazelnut spread, Vanilla ice cream, Potato chips, Popcorn, Digestive biscuit, Doughnut, Muffin, Gummy bears, Ketchup, Mustard, Mayonnaise, Pesto, Jam, Soy sauce, Muesli, Granola, Whey protein powder, Cornflakes, Baked beans, Falafel, Pizza Margherita, Spaghetti Bolognese, Lasagne, French fries, Mashed potato, Sushi roll, Pancake, Pork schnitzel (breaded), Frikadelle, Meatballs (beef), Bratwurst, Frankfurter, Leberkäse, Salami. The **31 branded (red)**: Coca-Cola(+Zero), Red Bull, Nutella, Milka, Haribo, Alpro, Barilla, Kellogg's, Philadelphia, Babybel, Actimel, Snickers/Mars/Twix/KitKat/Oreo/Pringles, Ben & Jerry's, McDonald's Big Mac/Cheeseburger/McNuggets/Fries/McChicken, BK Whopper, Döner Kebab, Currywurst, Subway, Domino's, Starbucks Latte, KFC. Branded/fast-food values are **best-effort estimates** (drift — barcode scanner is the accurate path); fast food stored per-100g + whole-item portion; **no photos on branded** (brand-logo licensing). The redundant generic "Cola" was deleted (branded Coca-Cola covers it). **Rule for adding foods: pick the badge — whole only if macros are the same regardless of source; generic if it's a processed/composite estimate; branded only for a named product.** Trainer foods still rank first (1100).
>    - **(B) Auto-saved scanned foods (no re-scan) — DONE.** New table **`scanned_foods`** (migration: `id, client_id→users, food_name, brand, source default 'off', source_id, nutrients_json, food_groups, image_url, scanned_at, UNIQUE(client_id,source,source_id)`; RLS `client_manage_own_scanned_foods` ALL `client_id=auth.uid()`). `components/FoodSearchModal.tsx`: **`handleBarcodeScanned` upserts the scan** into `scanned_foods` the moment a barcode resolves (before logging — so it persists even if not logged), then `openPortion({...food, scannedAt})`. New `FoodResult.scannedAt`; `loadScanned()` loads the client's scanned foods (newest first) → merged with custom foods into the **"My foods" tab** (`myFoodsAll = [...scannedFoods, ...customFoods]` → `filteredMyFoods`). `SourceBadge` shows a **`barcode.viewfinder` scan icon** when `scannedAt` is set (takes priority over the source icon); the portion card shows **"Scanned on <D Mon YYYY>"** (`fmtScannedDate` + `s.portionScannedRow`/`portionScanned`). Scope = **the client's OWN scans only** (per Vitek). Logging a scanned food also flows into `recent_foods` (now fixed) so it appears in RECENTLY ADDED too. **Possible enhancement (not done):** also merge scanned foods into the **All**-tab name search (currently reachable via "My foods" tab + recent, not by typing in All).
>    - **(C) Recently-used foods on opening search — FIXED (July 2026).** `components/FoodSearchModal.tsx` "All" tab empty state already RENDERS **"RECENTLY ADDED"** (10 most recent, `recent_foods` ordered by `last_used_at desc`, line ~699), but it showed nothing because `recent_foods` was **silently failing on TWO schema bugs** (same class as the old water_logs/favourite_foods bugs): (1) `handleAddFood`'s upsert writes a **`food_groups`** column that didn't exist on `recent_foods`, and (2) the `recent_foods_source_check` CHECK only allowed `off|usda|manual` — so **`trainer`/`custom` foods were rejected**. Both writes failed → table stayed EMPTY (0 rows app-wide). **Fixes (DB migrations, LIVE):** added `food_groups text[] NOT NULL DEFAULT '{}'` to `recent_foods`, and widened the source CHECK to `off|usda|manual|custom|trainer`. Also **backfilled** the client's already-logged trainer foods into `recent_foods` (from `food_log_entries` ⨝ `trainer_foods`, accurate per-100g) so recents show immediately. No app-code change needed for the write — every log now populates it. **Polish DONE (Vitek asked):** `loadRecent` now fetches **20** (was 10) and enriches each recent row with a photo — trainer foods from `trainer_foods.photo_url`, off/usda from `food_cache.image_url` — into a `recentImg` map, passed as `imageUrl` when rendering (`recent.map(r => renderFoodRow({ ...rowToFoodResult(r), imageUrl }))`). So the RECENTLY ADDED list now shows food photos. Vitek confirmed recency order is fine (no frequency/"most-often" needed). **Still open (minor):** recent trainer-food rows have grams-only portion (recent_foods carries no `portions`). These FoodSearchModal edits are LOCAL/uncommitted, JS-only (hot-reload), typecheck baseline stays 29.
> - **Where we ended / next after `/clear`:** DB seeding + photos are LIVE. Next: build features A/B/C (ask Vitek which first), optionally backfill the ~120 photoless foods + add the TheMealDB credit line, and commit the 2 local bugfixes when he approves (memory: never auto-commit/push). The food data is easily reversible (all tagged to his trainer_id).
>
> **▶️ RESUME HERE — Favourites LANDING redesign: 2×2 tile grid + separate trainer card (July 2026, COMMITTED & PUSHED).** `app/(client)/nutrition/favourites.tsx` landing only. Vitek wanted the old 5-stacked-`FullWidthCard` landing to distinguish *his* favourites from the trainer's Recommendations. Result: the **4 favourites (Recipes/Meals/Foods/Days) are a 2×2 `TileCard` grid** under a **"Your saved & shared"** label; **Recommendations is a separate full-width `FullWidthCard`** under a **"From your trainer"** label. Both card types were restyled to the **home-screen tile look** — large translucent watermark icon in the corner (`rgba(255,255,255,0.10)`), **decorative circles removed**, content spread top→bottom (title/desc top, count badge bottom-left, **`→` arrow absolutely positioned TOP-RIGHT** so it doesn't overlap the corner watermark), and **3-stop same-hue GRADUAL gradients** (`start {x:0.4,y:0}`→`end {x:0.6,y:1}`, copied from the home Nutrition/Training tiles). **Palette (settled over ~6 rounds — see CLAUDE-nutrition.md "Landing page"):** the 4 tiles are the "prime" colours amber/blue/green/red-pink in a deep muted register — Recipes amber `['#d4841e','#b06614','#8a4e0e']`, Meals indigo `['#33499a','#26386f','#1a2650']`, Foods green `['#347463','#255145','#183a30']`, Days plum `['#88376c','#682452','#4a1740']` (**Days icon = `calendar`, not heart**). **Recommendations is deliberately DIFFERENT + understated** — a bright rainbow was tried and rejected as "too much colour"; final is a **quiet dark brown/taupe** `['#453a30','#2f2820','#211b15']` so it recedes as the trainer card. **Header kept `plain`** (off-white) — the mint green-tint was tried here and reverted (still separate, pending the app-wide `plain` A/B). New styles: `tc` (TileCard), `s.tileRow`, `s.sectionLabel`; `fc` (FullWidthCard) lost its circles + went `justifyContent:'space-between'` height 132. The tile/card **`→` arrow is absolutely positioned TOP-RIGHT** (`tc.arrow`/`fc.arrow`) so it clears the corner watermark; count badge is bottom-left. **Recipes tile is amber now, so the recipe no-photo fallback gradient was flipped green→amber `['#d4841e','#8a4e0e']` in all 3 client recipe covers** (favourites `RecipeCard`, `recipe/[id].tsx` detail, `recipe/create.tsx` editor) to match. Typecheck baseline stays 29. All JS → hot-reload.
>
> **▶️ RESUME HERE — Insert a saved Favourite Day RETROACTIVELY (July 2026, COMMITTED & PUSHED).** Client complaint: on the Food Log you can add individual foods to a past day, but adding a whole **saved day** from Favourites always landed on *today*. Root cause: the Food Log FAB → "Add a day from Favourites" navigated to the Favourites tab and the insert hardcoded `new Date()`. Fixes (`app/(client)/nutrition/index.tsx` + `favourites.tsx`):
> 1. **Carry the selected day** — `handleInsertDay` now pushes `favourites?tab=days&insertMode=true&date=<toDateStr(selectedDate)>` (the week-strip's selected day), mirroring how the Training-tab week-strip `+` targets the selected day.
> 2. **Persistent-tab reactivity bug (the real trap)** — Favourites is a persistent `NativeTabs` screen, so navigating in with new params does NOT re-run `useState` initializers. The date was a one-time `useState(() => parseDateStr(dateParam))` → froze on the first mount's params (no date → today), so it always inserted to today; and `view` (also useState) stranded the user on the Favourites landing instead of Days. **Fix:** `insertDayDate = useMemo(() => parseDateStr(dateParam), [dateParam])` (reactive) + a `useEffect([isInsertMode, tabParam])` that syncs `view` to `'days'` in insert mode. New module helper `parseDateStr(s)` parses `YYYY-MM-DD` as a **local** Date (avoids `new Date(str)` UTC shift). **General rule: on a persistent native tab, read deep-link params reactively (useMemo/useEffect), never via useState initializers.**
> 3. **No redundant date picker** — the insert modal is now a plain confirm ("…will be added to Thu 16 Jul, keeping their original meal categories"), since the target day is fixed by the FAB. `insertDay()` writes to `toDateStr(insertDayDate)` then `router.navigate('/(client)/nutrition')` **immediately** (the Food Log reloads on focus and shows the items = the confirmation; no lingering toast).
> 4. **Single favourite FOOD add** (`openAddFood`) also defaults `addFoodDate` to `parseDateStr(dateParam)` so a favourite food logged from this flow lands on the selected day too. The Food Log's own FAB paths (food/meal/water) already saved with `selectedDate`. Confirmed working on Vitek's Expo build. Typecheck baseline stays 29. All JS → hot-reload. Full detail in CLAUDE-nutrition.md "Days list → Insert-a-saved-day flow".
>
> **▶️ RESUME HERE — Favourite Days sub-tab: expandable detail + redesigned card (July 2026, COMMITTED & PUSHED).** Client Favourites → **Days** list (`app/(client)/nutrition/favourites.tsx`). Vitek: the saved-day cards were "dull/functional" and the expand showed almost nothing. Work:
> 1. **Expanded detail actually works now** — the old expand filtered `meal_category === 'Breakfast'/'Lunch'/'Dinner'/'Snack'` (capitalized), but `food_log_entries.meal_category` stores **lowercase** keys + 5 typed snack subtypes → it matched nothing and rendered empty. Added module-level **`MEAL_SECTION_ORDER`** + **`groupDayEntries(snapshot_json)`** (ordered breakfast→snack_morning→lunch→snack_afternoon→snack_pre_workout→dinner→snack_post_workout→snack_evening→legacy `snack`; unknown/null → "Other" bucket) + **`portionLabel(e)`**. Each meal section renders a header (emoji + label + `N items · X kcal` subtotal) and per-item rows (name + portion left; kcal + `P·C·F` right).
> 2. **Card redesign** — collapsed header now: a **heart badge with the day-of-month number inside** (44px solid ACCENT `heart.fill` + white 14px/800 number, `heartDateWrap` `paddingBottom:5` so it sits in the lobes) as a "favourited date" marker; **colored macro pills** (`COL_PROT #378ADD`/`COL_CARB #EF9F27`/`COL_FAT #D85A30`, tinted `${COL}18` bg — the Weekly-screen palette, now also declared in this file); a prominent **kcal readout** (`dayKcal` 22px/800 + "KCAL"), `dayKcalWrap` `alignSelf:'center'` (vertically centered — was pinned top); the expand **chevron moved to a bottom-center row** (`dayChevronRow`) as a pull-to-expand affordance.
> 3. **Subtitle no longer repeats the date** — `day.name` defaults to the date label ("Thursday 9 July", user-editable), so `nameIsDate = day.name.trim() === dateLabel` → default-named days show `YYYY · N items` (year adds info the title lacks), renamed days show `dateLabel · N items`.
> 4. **Shadow fix** — the single-layer `itemCard` had `overflow:'hidden'` which clips the iOS shadow (the app-wide card rule). Split into **two layers**: `itemCardOuter` (shadow, no clip: `offset {0,5}, opacity 0.13, radius 16, elevation 6`; `itemCardOpen` → `0.17/20` when expanded) + inner `itemCard` (`overflow:'hidden'`, radius 16). Full detail in CLAUDE-nutrition.md "Days list". Typecheck baseline stays 29. All JS → hot-reload.
>
> **▶️ RESUME HERE — Do Mode collapsed-set summary + expanded note footer + new-note dot + `workout_sets` double-insert fix (July 2026, COMMITTED & PUSHED).** Vitek's VG-inspired asks. **Applied to BOTH Do Mode files** (`app/(client)/workout/[workoutId].tsx` + `app/(trainer)/client/[id]/workout/[workoutId].tsx` — client was built first + confirmed on a build; trainer was then mirrored identically) **plus a bug fix in `app/(trainer)/workout-builder.tsx`.** Typecheck baseline stays **29** (all pre-existing gesture-handler/`any`/`onOpenProgress` errors; no new ones). All JS → hot-reload (the builder change too — no native rebuild).
> 1. **Collapsed exercise card shows the sets as a one-liner** — under the exercise name (only when `!isExpanded`), e.g. `12 × 42kg   ·   8 × 46kg   ·   8 × 50kg   …`. Module-level helper **`buildSetsSummary(sets)`** (added after `calcTotal` in both files): filters `!isRemoved && !isDropset`, shows the value **as it sits on the card** (`weightKg || targetWeightKg` and `repsCompleted || targetReps` — Vitek: "show what's inside the card," no target-vs-last-performed logic), **first 3 sets then `…`**. Format `${reps} × ${weight}kg`; bodyweight → `${reps}×`. Style `collapsedSetsSummary` (12.5px `#7a7a7a`, tabular-nums). Rendered inside the name column after the `ogLabel`. Normal-mode supersets get it automatically (they render through `ExerciseCard`); the edit-mode `SupersetGroupCard` is drag-only and intentionally doesn't.
> 2. **Expanded card has a NOTE footer at the bottom** — after the photo row, a hairline-topped row: left = **`NOTE  ·  <date>`** label + the latest note text (≤3 lines); right = **`See all ›`** (`chevron.right`) which opens the full notes sliding panel (`onOpenInfo` → `ExerciseInfoModal`, where you read/write/delete). If no note: shows "No notes yet" + **`Add note ›`** (client: hidden entirely in `readOnly` view). Module-level helper **`latestExerciseNote(ex)`** = newest non-deleted note, **trainer note preferred over client note** (arrays are oldest→newest, so `[length-1]`). Styles `noteFooter*` (row is `alignItems:'center'` so the action vertically centers on the note — an earlier `paddingTop:12` hack was removed after Vitek flagged it as off-center). The note persists across sessions (notes are workout-level), so yesterday's note still shows today with its date.
> 3. **Green dot next to the exercise name for a "new" note** (8px `ACCENT`, more visible than the 6px Info-button dot; and visible while COLLAPSED, which the Info dot can't be since it's in the expanded action row). **"New" is cross-session + time-based, exactly per Vitek:** a note shows the dot only while it's **newer than the most recent COMPLETED session** — i.e. written since the client last trained this workout — and the dot **clears on its own once they complete another session** (the note text stays, with its date). Implementation: `NoteEntry` gained `createdAt?: string` (populated from `notes.created_at` at the exercise-note load; a note added mid-session has `createdAt` undefined → treated as new); the load's recent-completed-sessions query now also selects `created_at`, and `[0].created_at` is stored in new state **`lastCompletedSessionAt`** (client sets it next to `setViewedSessionDuration`; trainer next to `setSessionCount`). Threaded into `ExerciseCard` as prop `lastCompletedSessionAt` (client 2 call sites, trainer 3 call sites). Dot = `latestNote && (lastCompletedSessionAt == null || latestNote.createdAt == null || latestNote.createdAt > lastCompletedSessionAt)`. Both timestamps are Postgres-format strings from the same source → lexicographic compare == chronological (they're always hours/days apart, so fractional-second edge cases never bite). This **replaced** the earlier session-ephemeral `infoSeen`-gated name dot; the Info-button `showInfoDot` (change-indicator, `infoSeen`) is unchanged.
> 4. **BUG FIXED — `workout_sets` were being DOUBLED** (Vitek saw a 3-set exercise render `1,1,2,2,3,3`). **Root cause (confirmed via DB):** the doubled rows were inserted **2.4 ms apart, both `is_added_during_session:false`** → a double-fire of the **workout-builder** `handleSave` (NOT Do Mode — its inserts use `is_added_during_session:true`). In the update-in-place path both runs delete-then-insert `workout_sets` and interleave → 6 rows. **Fix:** added a synchronous re-entrancy guard **`savingRef`** (a `useRef`) in `workout-builder.tsx` — `handleSave` returns immediately if `savingRef.current`, sets it true, and a new **`finally { savingRef.current = false }`** resets it on every exit (so the conflict-prompt re-invoke still works). `saving` state alone couldn't guard (async). **Also cleaned the existing duplicates in prod** — deleted 15 dupe rows across 5 workout_exercises (kept earliest per `(workout_exercise_id,set_number)`); 0 dupe groups remain.
> - **Where we finished / next:** COMMITTED & PUSHED (July 2026). Typecheck-clean (baseline 29). The trainer mirror is untested on a real build (client side was confirmed). Vitek may still want: cross-session "new" tuned (e.g. only trainer notes trigger the dot, currently client notes can too), or the footer note choice changed (currently trainer-preferred). The set-summary on edit-mode superset cards was deliberately skipped.
>
> **▶️ RESUME HERE — Do Mode "Session in progress" leave-dialog + Apple-style Liquid Glass confirm cards (July 2026, COMMITTED & PUSHED).** Both Do Mode files (`app/(client)/workout/[workoutId].tsx` + `app/(trainer)/client/[id]/workout/[workoutId].tsx`). Two things: (1) **The leave-session dialog** now has a `message` subtitle + **three real buttons** — **"Leave — keep it running"** (green, reworded from "Leave for now" to make the background-running behaviour clear), **"Discard session"** (red), **"Keep going"** (grey `confirmSecondaryBtn`; promoted from a faint `cancelText` link to a proper button). (2) **The shared centered `confirmModal` card** (and the free-session-name modal — same `confirmBox` style) is a **Liquid Glass panel** via a module-level **`GlassPanel`** helper. **Latest tuning (matches Apple's Notification Centre glass):** the material is now the ADAPTIVE **`GlassView glassEffectStyle="regular"`** (auto-tints to the content behind, keeps a specular edge, stays genuinely see-through) — NOT the earlier flat `"clear"` glass + heavy `rgba(255,255,255,0.5)` white wash, which read as milky plastic. The white scrim is now a **whisper**, `const GLASS_SCRIM_OPACITY = 0.14` (the ONE knob — raise toward 0.22 for more legibility/frost, lower toward 0.06/0 for more transparency); `BlurView` fallback intensity bumped `18→30` to compensate. Panel still borderless, `borderRadius:38`, soft shadow via `confirmBoxShadow`, `centeredRoot` dim 0.38. **`confirmSecondaryBtn` ("Keep going") changed** `#f0f0ee`→**`#c8c8c2` + a `rgba(0,0,0,0.08)` hairline border** — the old near-white pill vanished against the light glass; the soft grey + border reads as a button while staying subordinate to the green/red (and holds up if the glass ever renders darker). **Buttons stay FULL colour + white/dark text** (translucent/tinted-glass buttons were tried and rejected as washed-out). **NOTE on transparency:** `regular` glass adopts the tone of whatever's directly behind it — over the pale blurred exercise cards the panel reads milky-white (correct/expected, like an Apple sheet over a white list); it looks much more see-through over high-contrast content. **Possible next tweak:** the title/body are DARK text — if `regular` ever tints dark over a dark cover image the text could get muddy; fix would be bump the scrim or flip text to white. Full detail in §5 "Back button behavior" + the "Confirm-modal card = frosted Liquid Glass" note there. Typecheck baseline stays 29. Real glass only shows in an iOS-26 build. **All JS — hot-reload; `expo-glass-effect`/`expo-blur` already in the binary.**
>
> **▶️ RESUME HERE — Trainer "Edit workout" full-edit + menu polish (July 2026, COMMITTED & PUSHED).** Vitek: on the trainer side a workout's ⋯ only let you Rename — he wanted to fully edit an existing workout (name, **type/category**, **post-workout stretch**, **cover picture**, exercises). The **workout builder already supported this** via the `editWorkoutId` param (edit-in-place, preserves session-log history by soft-removing exercises) — it just wasn't exposed. Changes:
> 1. **"Edit workout" added to all 3 trainer workout ⋯ menus** — Library (`(tabs)/library.tsx`), client all-workouts (`client/[id]/all-workouts.tsx`), and the client-profile Training tab (`client/[id]/index.tsx`). Each opens `router.push('/(trainer)/workout-builder?clientId=<owner>&editWorkoutId=<id>')` (client-scoped menus pass the profile clientId; Library passes the workout's own `clientId`). Icon `square.and.pencil`.
> 2. **"Rename" REMOVED from all 3 menus** (Vitek: "it's part of the edit feature, that's enough"). The old `startRename`/`confirmRename`/rename-modal code is left in place but unreachable (dead, harmless).
> 3. **Builder is now a TRUE pure-edit when opened without `scheduleDate`** (`workout-builder.tsx`): (a) **`handleSavePress` fast-path** — when `editWorkoutId && !scheduleDate && loadedWorkoutClientId`, it SKIPS the SaveSheet (no re-picking client/placement) and calls `handleSave` directly with an intent built from the loaded workout's own placement (`existing-routine` if `loadedRoutineId`, else `standalone`), so a pure edit can't accidentally unlink a routine workout. (b) New state `loadedRoutineId` + `loadedOrderIndex` (set in the `editWorkoutId` hydration); a pure edit preserves the existing routine **position** (`order_index = loadedOrderIndex`) instead of appending. (c) **Set targets:** hydration now pre-fills set rows from the workout's OWN `target_reps`/`target_weight_kg` for a pure edit — the last-performed pre-fill (`fetchLastPerformedMap`, blank if never done) is now gated to the **schedule** flow (`scheduleDate` present) only. Without this, saving a pure edit would overwrite the planned targets with last-performed values (or blanks). The schedule flow (`add-workout.tsx` → `editWorkoutId + scheduleDate`) is unchanged.
> 4. **Training-tab gallery cards' ⋯ now opens the FULL options panel** (was details-only). Previously the WORKOUTS-gallery card ⋯ opened `SessionDetailsSheet` directly; now it opens the `WorkoutMenuModal` (Edit workout · **Session details** · Add to Routine · Mark as done/Reactivate · Delete). "Session details" (relabelled from "View exercises", icon `list.bullet.rectangle`) is the option that opens `SessionDetailsSheet` (via `openViewExercises`, `{id,name,category}` — no session-specific stats, same as before). `activeMenu` state was widened from `WorkoutWithLastDate` to a minimal `{id;name;category;status?}` so both gallery cards (`WorkoutCard`) and the Recent Activity card (`WorkoutWithLastDate`) can open it.
> 5. **Training-tab `WorkoutMenuModal` converted from a centered `Modal` → `BottomSheet`** (slides up from the bottom, `close(handler)` pattern) — matching the all-workouts/Library menus + the app-wide menu convention. (`menuStyles.option` already has `paddingHorizontal:20` so rows fit the sheet.)
> 6. **Builder header → LIGHT** (Vitek: "no dark green header anymore, like the other screens"). The builder keeps its OWN in-flow `SafeAreaView` header (NOT the shared `LightHeader` — its right slot is a 44px icon slot too narrow for the text **Save** pill, same reason the meal/recipe editors keep custom headers): `root`/`headerSafe` bg `HEADER`(#244e43)→`BG`(#faf9f7), back chevron tint `#fff`→`HEADER` (`weight="semibold"`, size 22), title `#fff`/17/600 → `TEXT`/20/700 + dynamic **"Edit Workout"** (when `editWorkoutId`) / "Build Workout", `StatusBar` `light-content`→`dark-content`, `headerBar` gained a hairline bottom border. The green ACCENT **Save** pill is unchanged. Title is roughly centred by `space-between` (not dead-centre like `LightHeader`).
> Typecheck clean — baseline stays 29 (the pre-existing gesture-handler/any errors). **All JS changes — hot-reload, no native rebuild needed.**
>
> **▶️ RESUME HERE — Header GREEN TINT (A/B test, July 2026, COMMITTED & PUSHED).** Vitek liked the see-through glass header but wanted "a bit more character" — a faint brand-green wash — WITHOUT going back to the heavy dark-green bar. Done in **`components/LightHeader.tsx`** (glass variant only): the header's **tint layer** (which was decoupled off-white `rgba(250,249,247,…)`) is now a **light MINT** `const TINT = '150,201,178'` (rgb string). **Dark-green tint was ruled OUT** — the header glyphs/title are dark-green `#244e43`, so a dark tint kills contrast. Two structural changes made the green read well: (1) the **blur is now clipped in its OWN inner `overflow:'hidden'` container** (still ends crisply at the header edge — content stays sharp), and the **`lh.wrap` no longer clips** so (2) the **green tint gets its own gradient with a LONG tail** that extends **`const TINT_DROP = 70`px BELOW the nav row** (5 stops `0.95→0.7→0.4→0.15→0`, reaching exactly 0) — it washes gently down into the page instead of cutting off at the title (the "green finished too soon" fix). The green is rendered **ON TOP of** the blur (light frost would otherwise wash it out). **Tunable knobs:** `TINT` (hue/depth), `TINT_DROP` (tail length). Because the tail overlaps the top of content, **Appointments + Me** bumped their scroll `paddingTop` to **`headerH + 14`** (was `headerH`) so their first white CARD starts below the wash (Training didn't need it — its first content is the transparent gauge). **A/B PROP:** added **`plain?: boolean`** to `LightHeader` — renders the **original neutral off-white tint** (short fade within the header, no green) for a see-through comparison. **All 5 nutrition screens pass `plain`** (`nutrition/index`, `favourites`, `weekly`, `grocery-list`, `recommendations`) so testers compare green (main tabs + training sub-screens) vs plain (nutrition). **Solid headers unaffected** (trainer tabs + client-detail — opaque, no tint layer). Typecheck clean (baseline 29). **Next:** once testers weigh in, make it uniform (green everywhere or plain everywhere) by flipping/removing the `plain` props.
>
> **▶️ RESUME HERE — Favourites + Recipe polish (July 2026, NOT yet committed/pushed).** This session touched **3 files** (`app/(client)/nutrition/favourites.tsx`, `app/(client)/recipe/create.tsx`, `app/(client)/recipe/[id].tsx`) — all typecheck clean, no new tsc errors (baseline stays 29 unrelated). **These changes are LOCAL ONLY — review + commit/push when ready** (Vitek left before I finished; he'll pick up after `/clear`). What changed:
> 1. **Meals sort pills removed** — the Newest/Oldest/A–Z/Z–A row is gone; meals now always sort **alphabetically**. (Vitek: "alphabetically by default and that's enough.")
> 2. **Recipe editor `recipe/create.tsx` REBUILT to mirror the meal editor** — draft-first + auto-save, frosted `LightHeader`, cover card below header, name/portions rows, per-portion nutrition strip, meal-style ingredient rows (swipe-remove + tap-edit-amount), instructions sheet, Share-with pills, Save/Delete. Now takes **`?id=<recipeId>`** (+ `?isNew=1`); the old `?editId=` is gone. Favourites "+" → new `startNewRecipe()` inserts a draft row then opens the editor. **Logging stays on the recipe detail page** (recipes have one, unlike meals). See CLAUDE-nutrition.md "Recipe editor screen".
> 3. **Recipe detail `recipe/[id].tsx` REDESIGNED** — frosted `LightHeader` (name in header) + **cover as a card below the header** (like meals; was a hero with overlaid name/buttons); the owner **⋯ menu is now a slide-up `BottomSheet`** (was centered modal); Delete confirm → centered modal (was `Alert.alert`). (Vitek's 2 screenshots: "picture under the header like meals; ⋯ should slide from the bottom.") See CLAUDE-nutrition.md "Recipe detail screen".
> 4. **Favourites search bars FOLD under the header (WhatsApp-style)** — in Recipes/Meals/Foods/Recommendations the toolbar (search + `+`, filter pills, or the Recomm. tab switcher) now lives **INSIDE the sub-view ScrollView** (was a fixed bar above it, so cards slid *behind* the search bar and never reached the frosted glass). New `s.emptyScroll` style for in-scroll empty states. See CLAUDE-nutrition.md "Search bar / toolbar FOLDS under the header".
> - **We DECIDED NOT to add a FAB to Favourites/Grocery** (Vitek's earlier idea) — redundant with the one-tap-into-sub-view + existing `+`; keeping the FAB unique to the Food Log so it stands out. Grocery already has its own add.
> - **Possible next tweaks Vitek may raise:** none open that I know of; verify the fold effect + recipe editor/detail on a real build.
>
> **▶️ RESUME HERE (nav redesign — client side DONE, July 2026).** Client nutrition + main tabs are fully migrated & **all pushed**: tabs → `NativeTabs`, headers → `LightHeader`, recipe + meal editors → `(client)` stack routes, round-2 spacing fixes, round-3 meal-editor polish (A–C), round-4 native-tab spacing + bar-retention (D–F). **Everything is committed AND pushed** — `git log origin/main..HEAD` should be empty. Vitek confirmed round-4 in a build ("works").
>
> **▶️ RESUME HERE — Trainer tabs → `NativeTabs` DONE (July 2026, LOCAL ONLY, not committed).** `app/(trainer)/(tabs)/_layout.tsx` rewritten from `<Tabs>` to `<NativeTabs>` (5 triggers: clients/schedule/library/finance/account; **`tintColor="#24ac88"` (ACCENT green — matches the client side; Vitek asked for accent in the bottom nav, was `#244e43`)**; `schedule` uses `sf="calendar"` — no `.fill`). **Unlike the client tabs, there is NO shared glass header** — each trainer tab screen renders its OWN in-flow dark-green `SafeAreaView` header (TrainerLogoButton), so the TOP needed no changes; only the BOTTOM did (native glass bar is transparent → content flows under it). Added **`useTabBarHeight()` (from `@/components/FloatingTabBar`) + `paddingBottom: tabBarH`** to every scroll/list in all 5 screens: clients FlatList; finance invoice-FlatList + earnings-ScrollView; account scroll (also dropped its `SafeAreaView edges={['bottom']}` → plain `View`); library exercises-SectionList + WorkoutsTab (both ScrollViews) + NutritionTipsTab + FoodsTab (list `tabBarH+40`, **FAB moved to `bottom: tabBarH+16`**) + RecipesTab; schedule day-grid ScrollView + WeekView ScrollView + inline month-grid (`cal.grid` `paddingBottom: tabBarH`). Typecheck clean — baseline stays 29 (the 6 account.tsx errors are pre-existing gesture-handler/any errors, line numbers shifted +2). **Verify on an iOS-26 build (glass + vibrancy + content clears the bar), then commit/push.**
>
> **▶️ RESUME HERE — Trainer HEADERS → light `LightHeader` (stage 1: client-detail DONE — round-3, July 2026, COMMITTED & PUSHED).** Vitek: "same system as client side; in the client-detail the header stays and the [5-tab] nav is on top." **⚠️ The glass-pill wording below (round-1/round-2) is SUPERSEDED by round-3 — the main tabs are now a plain UNDERLINE switcher and the glass pill moved down to the sub-tabs (see round-3 paragraph + the "Client profile layout" section).** **Done on `app/(trainer)/client/[id]/index.tsx`:** dark-green `SafeAreaView` header → **`LightHeader solid`** (back chevron · client name · `plus`; `StatusBar dark-content`; root bg `HEADER`→`BG` #faf9f7). Session timer → `LightHeader` `overlay` slot (`hdrSessIndicator`, tap → `resumeSession()`). The 5-tab underline switcher → a **`TabPillSwitcher`** component (module-level in the file): a pinned strip at `top: headerH` (`useHeaderHeight()`) with **NO outer track** — just the 5 titles + a **single glass pill that PHYSICALLY SLIDES** (`Animated.spring` on `translateX`, native driver) to the active tab. Pill = `GlassView glassEffectStyle="regular"` (from **`expo-glass-effect`**, real Liquid Glass on iOS 26) over a white base (`segPill`/`segPillInner`), gated by `isLiquidGlassAvailable()`; frosted-white fallback elsewhere. Labels: inactive **black** (`TEXT`), active **accent green** (`ACCENT`). Track width measured via `onLayout` → `itemW = trackW/5`; pill `translateX = idx*itemW + PILL_INSET(3)`, width `itemW-6`. Content pads `paddingTop: headerH + SEG_STRIP_H(56)` + `scrollIndicatorInsets` + RefreshControl `progressViewOffset`.
> **Round-2 polish (all from Vitek's build screenshots):** (1) **header is `solid` NOT see-through** — the translucent glass ghosted the dense week-strip underneath (messy); added a **`solid?: boolean` prop to `components/LightHeader.tsx`** = opaque `#faf9f7` header, no blur/mask/tint (client side keeps the glass — unaffected). (2) **title nudged up** — solid header's row height is `HEADER_ROW_HEIGHT - 10` so the title sits ~5px higher (was "too low"). (3) **switcher sits lower** (`segStrip` paddingTop 12) + no track pill. (4) **bottom tab bar tint → ACCENT green** (`_layout.tsx`). (5) **Nutrition + Progress sub-tab switchers → plain underline text** (like the client-side Progress switcher), NOT the pill toggle: **Progress** just passes `variant="client"` to `ProgressTab` (already supported); **Nutrition** (`nutrition-tab.tsx`) — restyled its `switcher*` styles to the underline pattern (`gap:32`, `borderBottomWidth:2` ACCENT active, 17px, `#bbb` inactive / `TEXT` active). Typecheck clean — baseline 29 (the client/[id]/index.tsx errors are pre-existing gesture-handler/any).
>
> **Round-3 — switcher redesign + spacing (Vitek's build feedback, July 2026, THIS is the current state):** the sliding glass pill on the **main 5-tab row was too heavy** and clashed with the sub-tab underline stacked right under it. Fixed: (1) **Main tabs → plain UNDERLINE** (`TabPillSwitcher` rewritten — active = accent text + 2px accent underline hugging the label, inactive black; `segPill`/`segPillInner`/`PILL_INSET` + the `Animated`/`expo-glass-effect` imports removed from `index.tsx`; `SEG_STRIP_H` 56→50). (2) **Sub-tabs → a GLASS TOGGLE** — new reusable **`components/GlassToggle.tsx`** (faint frosted track + sliding `GlassView` pill / frosted-white fallback), so the glass migrates DOWN one level where a 2-item pill reads as tasteful. Used by **Nutrition** (`nutrition-tab.tsx` — replaced the underline `switcher*` with `<GlassToggle>` + `s.subToggle`) and **Progress** (new **`variant="glass"`** branch in `progress-tab.tsx`; `index.tsx` now passes `variant="glass"`). **Client-side Progress unchanged** (`variant="client"` underline). (3) **Spacing:** `const segTop = headerH - 12` pulls the switcher + content up (tighter gap under the title); content pads `segTop + SEG_STRIP_H + 12` for breathing room below the tabs (indicator insets stay at `segTop + SEG_STRIP_H`). Net hierarchy: **underline = primary, glass toggle = secondary.** Typecheck clean (baseline 29). **Template pattern: "solid light header + pinned UNDERLINE main nav + `GlassToggle` sub-nav."**
>
> **✅ DONE (trainer headers stage 2, July 2026, LOCAL ONLY — not committed): the 5 trainer TAB screens** (clients/schedule/library/finance/account) migrated from dark-green `SafeAreaView` headers → **`LightHeader solid`**. Pattern applied to each: dropped the `SafeAreaView` header block + its `StatusBar barStyle="light-content"`; `StatusBar` → **`dark-content`**; root bg `HEADER`(#244e43) → **`#faf9f7`**; the `<LightHeader solid …/>` is rendered **LAST in the root View** (so it overlays), with **`left={<TrainerLogoButton light />}`** (new **`light` prop** on `components/TrainerLogoButton.tsx` — renders the VF glyph in dark green `#244e43` instead of white + a light hairline ring on the badge via `ls.badgeLight`; ALL the notif-badge/session/modal logic is unchanged), `title` = the screen name (**clients uses the `Hi <name>` greeting**), and `right` = a `HeaderIcon` wrapping `<SymbolView name="plus" size={22} tintColor={HEADER_ICON} weight="semibold"/>` for the four screens that had a `+` (account has **no** right slot). Content offset: each screen's first content element pads `paddingTop: useHeaderHeight()` (**clients/schedule** on their `content` wrapper View; **library/finance** on the `segmentWrapper` — `headerH + 14` / `headerH + 12`; **account** on the scroll `contentContainerStyle` — `headerH + 8`) — on TOP of the existing `paddingBottom: tabBarH`. The old `headerSafe`/`headerBar`/`headerTitle`/`addButton*` styles were deleted from all 5. **The `+` right-slot is now dark-green (`HEADER_ICON`), NOT the old plain-white `+`** (white would be invisible on the light glass). Library's `+` keeps its segment/sub-tab-aware routing (exercises→add-exercise, workouts→workout-builder, nutrition→recipe-create / foods tick / tips tick).
> **Round-2 (from Vitek's build screenshots — same session):** (a) **`plan-week.tsx` (Planning)** — the ONE trainer screen still on the dark-green header — was ALSO migrated to `LightHeader solid` (it's a pushed stack screen, so **left = back `chevron.left` `HeaderIcon` → `router.back()`**, title "Planning", right = the `+` add-appointment `HeaderIcon`; `infoBar` padded `headerH + 10`; root bg → `#faf9f7`; dead `headerSafe`/`headerBar`/`headerSide`/`headerTitle`/`headerAdd` styles removed). (b) **Library + Finance switchers unified to the client-detail hierarchy** (Vitek: "library should have the same set up as client detail; make [Finance's pill] white/glass like the rest"): **Library main tabs (Exercises/Workouts/Nutrition) → plain UNDERLINE** (new `styles.mainTab*` — active = accent text + 2px accent underline, `fontSize:15`; was the dark-green Type-1 pill) and **both Library sub-tab switchers → `GlassToggle`** — Nutrition's Recipes/Recomm./Tips/Foods (4-item) and Workouts/Templates (2-item), each styled `wStyles.subToggle` (`marginH 16, marginTop 12, marginBottom 6`); the old `wStyles.underlineTab*` + `styles.segment*` were removed. **Finance's Invoices/Earnings → plain UNDERLINE** (`st.mainTab*`, same as the Library main tabs — round-3 correction: a `GlassToggle` was tried first but Vitek wanted the underline). Net trainer-side pattern is now uniform: **solid light header → UNDERLINE primary nav → `GlassToggle` secondary nav**.
> **Round-4 (glass "goes dark in the evening" — ROOT CAUSE FOUND: system dark appearance).** Vitek saw the **native tab bar + `GlassView` toggles (real iOS 26 Liquid Glass) go dark**, on BOTH sides, **only in the evening**. **Real cause:** the app was `"userInterfaceStyle": "automatic"` in `app.json`, so it followed the **system** appearance — and iPhones on **Display → Automatic flip to Dark at sunset**. The app's own JS views are hardcoded light so they look unchanged, **but native Liquid Glass surfaces are appearance-aware and render DARK in Dark mode** → dark nav/toggles at night, clearing briefly on tap (touch forces a re-composite). It was never about the *number* of glass surfaces (the earlier "too many GlassViews perturb the compositor" hypothesis was wrong; the Library toggles just added more dark surfaces to notice). **FIX: `app.json` → `"userInterfaceStyle": "light"`** — locks `UIUserInterfaceStyle = Light` so the whole app (incl. native glass) always renders light regardless of system/time. **⚠️ Native config change → needs a NEW BUILD (no hot-reload / OTA).** **Vitek confirmed the light-lock fixed it on a build.** The **`GlassToggle` `frosted` prop** (added in the wrong-hypothesis step — forces the frosted-white pill, no real `GlassView`, via `const glass = isLiquidGlassAvailable() && !frosted`) was then **reverted off the two Library toggles** → they're back to **real glass**, matching the client-detail toggles. The `frosted` prop is KEPT on the component (unused now, harmless — handy if a glass surface ever needs to opt out again). **The app must STAY `userInterfaceStyle: "light"` until a real dark theme is built** — flipping back to `automatic`/`dark` before then would make the native glass go dark at night again with no matching app-side dark styling. When dark mode IS built (every hardcoded colour needs a light/dark variant), flip `userInterfaceStyle` back to `automatic`.
> **Round-3 (Vitek's next round of build feedback, same session):** (a) **Finance Invoices/Earnings → underline** (see above, replaced the round-2 GlassToggle). (b) **Finance filter pills active colour → light-green `ACCENT`** (was dark-green `HEADER`) — both the invoice status pills (`invFilterPillActive`) and the earnings time-range pills (`pillActive`). (c) **Library resets its nav on LEAVE** — a second `useFocusEffect` whose blur-cleanup sets `segment='exercises'` + `nutSubTab='recipes'` + `workoutSubTab='workouts'`, so returning to the Library tab always lands on the first tab/sub-tab, never where you left ("you're not continuing the same work"). **Finance got the same treatment** — a blur-cleanup `useFocusEffect` resets `activeTab='invoices'` so it always reopens on Invoices, never Earnings. Typecheck clean — baseline stays 29 (the account.tsx + client/[id]/index.tsx errors are the pre-existing gesture-handler/any ones). **Verify on an iOS-26 build (solid headers sit cleanly; no ghosting; the 4-item Nutrition glass toggle reads well; Library resets on tab-switch; `+`/back/TrainerLogoButton taps work), then commit/push.**
>
> **⬜ THEN: retire `FloatingTabBar`** — move `useTabBarHeight()` to a tiny shared module (now consumed by client-tab + nutrition + all 5 trainer-tab screens), then delete the unused `FloatingTabBar` component. See the ⬜ item in the nav blockquote.
>
> **Nutrition — DONE & pushed:**
> 1. ✅ **Recipe open/create nav fixed** — moved `recipe/[id]` + `recipe/create` OUT of the nutrition tabs to `(client)` stack routes (`app/(client)/recipe/…`), removed hidden triggers, nav paths → `/(client)/recipe/X`, `tabBarH` → `insets.bottom`.
> 2. ✅ **Meal editor extracted** to its own frosted stack route `app/(client)/meal/[id].tsx` (was an in-file overlay in `favourites.tsx`). Frosted `LightHeader` (title = meal name / "New meal", updates on rename); **cover is a rounded CARD that starts BELOW the header** (`marginH 16, height 180, radius 16` — NOT a hero bleeding into the header; frosted look only on scroll); scroll uses `contentInsetAdjustmentBehavior="never"` + `paddingTop: headerH`. Layout: cover → **NAME row** → kcal/P/C/F → INGREDIENTS (+ Add food) → NOTES → SHARE WITH → **Save meal** (filled) / **Log this meal** (outline) / Delete. Auto-saves via `savePatch`; `?isNew=1` discards an empty+unnamed draft on back. Favourites `router.push`es to it; reloads meals on `useFocusEffect`. Old overlay + `md` styles + meal handlers/state removed from favourites.
>
> **Nutrition — round-3 (DONE & pushed):**
> - **A. Save button added** to the meal editor (`meal/[id].tsx`): **Save meal** (filled ACCENT → `router.back()`) is now the primary; **Log this meal** became the outline secondary; Delete unchanged. (Vitek: "we are missing a save button.")
> - **B. `components/FoodSearchModal.tsx` header → LIGHT** (was dark-green `#244e43`). Now `#faf9f7` bg + hairline bottom border, dark-green `xmark` + dark title, `StatusBar dark-content`. This is the SHARED add-food screen (food log, meal, recipe, favourites) — Vitek wanted it consistent with the frosted nav. (It's a `Modal` with a search bar right under the header, so it's a solid light header, NOT the scroll-under frosted `LightHeader`.)
> - **C. Meal cards → cover-image cards** (`mc` styles in `favourites.tsx`) matching the recipe cards + workout cover-card shape (were horizontal thumb+text rows). Now `height 130, borderRadius 14`, cover image / indigo gradient + dark bottom gradient, name + "N items · kcal · P/C/F" overlaid. Recipe cards (`rc`) were already this shape. (Vitek: "meal cards and recipe cards same shape as the workout cards.")
> - **STILL OPEN / possible next tweaks Vitek may raise:** he praised the header-shows-name behavior but there's now BOTH a header title AND a body "MEAL NAME" card (mild redundancy — left as-is intentionally; the body card is the rename control since `LightHeader`'s title isn't tappable). If he wants the name ONLY in the header, make the header title tappable to rename (needs a `LightHeader` tweak or a custom header) and drop the body NAME card.
>
> **Round-4 — native-tab spacing + bar-retention (DONE & pushed; Vitek confirmed "works"):**
> - **D. Progress + Me "content starts too low" FIXED (real cause found).** Both `(tabs)/progress.tsx` and `(tabs)/me.tsx` wrapped their scroll in **`<SafeAreaView edges={[]}>`**, which — under the native tab bar — injects a phantom top inset (~safe-area height) EVEN with `edges={[]}`. The round-2 `contentInsetAdjustmentBehavior="never"` did NOT cure it because the wrapper was the cause. **Fix: match `train.tsx` exactly — plain `<View>` root, and dropped the `never` override** (train has neither and is correctly spaced). `train.tsx` + `schedule.tsx` were already plain `View` → untouched. **RULE: client native-tab screens must use a plain `<View>` root, NEVER `SafeAreaView` — it double-insets the top under `NativeTabs`.**
> - **E. all-workouts + all-routines now KEEP the native bottom tab bar — via a NESTED STACK in the Training tab.** Vitek's rule: a workout LIST must behave like a meal LIST (bottom bar stays); only *opening* a workout (session-intro/Do Mode) hides it — mirroring meals. **The first attempt (hidden `NativeTabs.Trigger`s + `router.push`) FAILED silently — you CANNOT `router.push` to a hidden native-tab trigger** (the "See all" chevrons did nothing; this is the SAME wall that forced recipe screens out of the nutrition tabs). **Correct pattern: the Training tab is now a NESTED STACK.** `train.tsx` → **`app/(client)/(tabs)/train/index.tsx`**; `all-workouts` + `all-routines` moved to **`app/(client)/(tabs)/train/all-workouts.tsx`** + `train/all-routines.tsx`; new **`train/_layout.tsx`** = `<Stack screenOptions={{ headerShown:false }} />`. Because they're stack screens INSIDE the tab, `router.push('/(client)/(tabs)/train/all-workouts')` keeps the native bar. Both own a `LightHeader`, so the shared `ClientTabHeader` is **gated off by the LEAF segment**: `_layout.tsx` computes `leafRoute = segments[segments.length-1]` and `showSharedHeader = !leafRoute || !NO_SHARED_HEADER.has(leafRoute)` (`NO_SHARED_HEADER = {'all-workouts','all-routines'}`); `rawRoute` (segment after `(tabs)`) still drives the tab TITLE. Both list screens gained `useTabBarHeight()` → `paddingBottom: tabBarH` (+ `scrollIndicatorInsets.bottom`). The 5 nav paths in `train/index.tsx` point at `/(client)/(tabs)/train/all-{workouts,routines}`. Other refs to `/(client)/(tabs)/train` (home hero, Session/Stretch-Complete `router.replace`) still resolve to `train/index`. **RULE: to give a native-tab screen deeper pushable sub-screens that keep the bar, make the tab a FOLDER with its own `_layout` Stack — do NOT use hidden triggers + push.** **Availability, routine detail, past-sessions deliberately stay bar-less stack routes** (Vitek confirmed).
> - **F. Food-log `PearIcon` matched to the training-tab kettlebell** — `PearIcon` gained a `strokeWidth` prop (default `1.0`, so the home-hero watermark + `NotificationOverlay` pears are unchanged); the `nutrition/index` header instance is now `<PearIcon size={34} strokeWidth={1.5} />` (was `size={30}`, hardcoded stroke `1.0`) to match `<KettlebellIcon size={34} strokeWidth={1.5} />` — same viewBox `0 0 24 24`, so identical size + thickness, matching the solid VF mark's weight. (Vitek: "make the pear same thickness/size as the kettlebell.")
